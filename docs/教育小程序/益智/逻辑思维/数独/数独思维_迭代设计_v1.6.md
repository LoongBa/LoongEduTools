# 数独思维 · 迭代设计 v1.6 — 错题本/收藏本 + 提示带讲解 + 分享体系

> 对齐《数独小侦探_开发方案》§5 教学体系第三层（题面引导 v2）+ 家长端（E2）+ 产品迭代规划（v1.6 纯本地双收益；分享题/分享成绩为后续分期）。
> 状态：**v1.6 已实施（2026-09-25）**——错题本/收藏本 + 提示带讲解（§3/§4/§5）；**v1.7 已实施（2026-09-25）**——分享题文本导出/复制 + 打印图片（题面/答案切换）+ 导入题目（校验/直接开局/导入局不计成绩，§6.1/§6.2，实施版文件：`数独思维-设计文档.md` §2.9）；**v1.7.1 已实施（2026-09-25）**——分享成绩（§7.1/§7.2：canvas 成绩卡 + 无竞技文案复制，实施版 §2.10）；**v1.8 已实施（2026-09-25）**——交叉排除技巧教学关（L4，行/列/宫三源交集判定，四技巧全齐）；**v1.9 已实施（2026-09-25）**——家长报告（E2：今日反馈/近 7 天/技巧掌握度，只读本地）+ 9×9 小屏适配（E4：横屏提示/窄屏字号）；**v1.10 已实施（2026-09-25）**——自由解题模块（进阶技巧教学关：唯一余数 uniqueElim N6 唯一解盘 + X-Wing xwing N6 矩形锁 4，独立 advSkills ✨ 徽章，对齐开发方案 §1 L5+ 高阶收进自由解题模块，适龄徽章墙/家长报告计数保持 X/4）+ E5 自适应降难度（最近 2 局连败温和降档，`suggestedName` 内存非持久化零 schema，结算 note + 难度页橙色横幅 + 建议档按钮高亮，进入对局即清除，不惩罚）；**v1.11 已实施（2026-09-25）**——二维码（§6.3 落地：内嵌 qrcode-generator MIT，分享题弹层二维码卡 + 微信「长按识别图中二维码」闭环，绕开 JS-SDK 鉴权）+ 分享成绩微信文案适配（§7.3 结论=维持图片+文字为终态）+ 双指缩放（E4 P2 落地：捏合 scale 1~2.2 + 两指中心锚点 + 固定复位浮标，闭包落子无需坐标映射）；微信 JS-SDK 增强与双指平移（真机评估）为后续待实施；**v1.12 已实施（2026-09-25）**——每日挑战 + 成就（X3 落地：当日同题 `mulberry32` 种子注入 + `newRound(seed)` solver.js 零改动、难度页每日挑战卡 + `startDaily()` 固定 6×6 + `state.isDaily` 标记 + `store.daily` 记录；6 枚成就（首通/连续3·7天/十局/技巧大师/三星完美）`checkAchievements()` 结算解锁 + 「🏅 解锁成就」note + 成就视图）。适用：数独思维（已由「数独入门」更名）；**v1.13 已实施（2026-09-25）**——闯关地图（X1 落地：3 档×3 关 9 关线性推进，`MAP_LEVELS` seedBase 固定 → 复用 v1.12 `newRound(seed)` 同关同题；`state.fromMap` 顶栏「· 关卡 X-Y」+ `store.mapProgress.completed` 进度记录；地图视图 `showMapView` 三态 ✅/🔓/🔒 + 9/9 全通 🎉；无竞技纯个人推进）；**v1.14 已实施（2026-09-25）**——设置面板（音效开关 `soundEnabled` + `tone()` 单点拦截 + `store.settings.sound` 零迁移持久化；清除本地进度确认浮层 + `localStorage.removeItem` 重建默认 store；关于版本与隐私说明「无账号无云同步，数据只存本机」）；**v1.15 更名（2026-09-25）**——「数独侦探」→「数独思维」（教育语境，避免「侦探」游戏感误会；目录/文档/入口/产物名全量同步 git mv 保留历史；成就「十局小侦探」→「十局小达人」、闯关 3-3 tip「大侦探！」→「思维高手」等称号中性化）；**v1.16 已实施（2026-09-25）**——家长报告成长进度卡（每日挑战今日完成+连续打卡 / 成就 X/6+最近解锁 / 闯关 X/9+当前档位，全派生零 schema）+ 技巧掌握度列表修复（仅列适龄 4 项，与分母一致）；**v1.17 已实施（2026-09-25）**——每日挑战难度轮换（`DAILY_DIFFS` 按星期：周日 6 / 周一·周二 4 / 周三-五 6 / 周六 9，`startDaily` 当日难度 + 当日种子同题，`store.daily.level` 记实际难度）+ 成就扩充 2 枚（`mapMaster` 闯关 9 关全通 / `monthStreak` 连续打卡 30 天，ACHIEVEMENTS 6→8）。；**v1.18 已实施（2026-09-25）**——闯关×技巧徽章联动（X1「复用徽章数据」落地：`MAP_LEVELS` 每关标 `skill` 主考技巧（boxElim×3 / rowColElim×2 / blockElim / crossElim / uniqueElim / xwing），节点技巧 tag ✅/🎯（与徽章墙视觉一致，不用 🔒 避三态混淆），adv 关未掌握点关 → 温和提示 + 跳自由解题对应教学关（不硬锁），**首次通关实战点亮徽章**（base→`store.skills` / adv→`store.advSkills`，adv 绝不混入 base）+ 结算 note「🎓 实战点亮技巧徽章」，completed 追加 `doneAt` 零迁移（旧数字项兜底），闯关卡 sub「通关 X/9 · 掌握技巧 Y/6」去重计数）+ 成就扩充 2 枚（`mapStreak` 连续 7 天闯关 / `favorite` 收藏 10 题，ACHIEVEMENTS 8→10，`mapStreakDays()` 派生 doneAt 连续天数）+ 每日挑战打卡卡（`.streak-card`：连续大字 + 今日状态 ✅/🎯 + 本周 7 日圆点实心/空心/灰，全派生 `checkin.dates` 零 schema）。；**v1.19 已实施（2026-09-26）**——X2 生成引擎独立化文档化（纯文档轮，solver.js 零改动）：新增《生成引擎独立模块说明.md》（模块契约 + 迁移指南：7 方法 API / cellValid 空格前提 / genPuzzle 已知格 [givens,givens+4] 唯一解保证 / 种子注入模式（mulberry32 包裹 Math.random，v1.12/v1.13 同题机制）/ 与 24点 solver.js·打字背单词 keyboard.js 同源「独立可复用」惯例 / 迁移三路径（原地→系列级 _shared→仓库级 _shared，命名冲突改 lx-sudoku.js）/ 线二映射（数独生成器方案 §5.1-5.3 ↔ fillBoard·genPuzzle·solveCount））+ 设计文档 §3.2 补独立模块声明 + 工具版本 1.18→1.19（tools.py，UI 顶栏/关于同步）+ 冒烟断言版本同步（smoke_shudu A2 / smoke_shudu_settings B6）。；**v1.20 已实施（2026-09-26）**——闯关×技巧徽章联动反向延伸（补缺口）：成长进度卡（卡片 C）闯关行补「掌握技巧 Y/6」（复用 litMapSkillCount 去重计数 + 分母动态 SKILLS.length，与难度页闯关卡 sub 口径一致）+ 成就 10→11 枚新增「六艺精通」（sixArts：litMapSkillCount() >= SKILLS.length，点亮全部 6 枚技巧徽章——适龄 4 教学关 + 进阶 2 自由解题任一来源皆计，闯关实战点亮同样计入）；计数/视图/成长卡成就行 auto X/11；solver.js 零改动、零 schema。；**v1.20 已实施（2026-09-26）**——闯关×技巧徽章联动反向延伸（补缺口）：成长进度卡（卡片 C）闯关行补「掌握技巧 Y/6」（复用 litMapSkillCount 去重计数 + 分母动态 SKILLS.length，与难度页闯关卡 sub 口径一致）+ 成就 10→11 枚新增「六艺精通」（sixArts：litMapSkillCount() >= SKILLS.length，点亮全部 6 枚技巧徽章——适龄 4 教学关 + 进阶 2 自由解题任一来源皆计，闯关实战点亮同样计入）；计数/视图/成长卡成就行 auto X/11；solver.js 零改动、零 schema。；**v1.21 已实施（2026-09-26）**——分享成绩卡补技巧掌握 X/6（drawResultCard 徽章行 + shareResultText 第 3 行：countLitSkills() base 口径 → litMapSkillCount() + 分母动态 SKILLS.length，单行「掌握技巧 X / 6 🏆」/空态「技巧徽章待点亮 💪」，对齐闯关卡 sub 与成长卡口径）+ 成就 11→14 枚（allStars「全档三星」store.best 4/6/9 各 ≥3★ / daily14「坚持两周」streak ≥14 / advBoth「进阶双修」uniqueElim+xwing 进阶徽章全亮，全零 schema 派生）；计数/视图/成长卡成就行 auto X/14；solver.js 零改动。；**v1.22 已实施（2026-09-26）**——分享题卡补技巧标签（闯关来源诚实标注：`drawPuzzleImage` 标题下追加「技巧标签：{名}」行 y=62 + `shareTextForCurrent` SD 行后追加同文本行，仅 `state.fromMap >= 0` 取 `MAP_LEVELS[fromMap].skill` → `skillByKey` 显示名，普通/导入/回放局不虚构检测——solver.js 无逻辑求解器；`parsePuzzleText` 只取 SD 行忽略其它行，技巧标签行不破坏导入向后兼容）+ 成就 14→15 枚（`clearAll`「全档挑战」：`store.best['4'|'6'|'9']` 各至少通关一次，零 schema 派生，与 allStars 构成入门→精通梯度）+ 家长报告卡片 B 技巧掌握度**进阶分区**（适龄 X/4 保持不动，进阶 2 枚独立展示 ✨/💎 + X/2「进阶技巧已点亮」，adv 不混入适龄分母，数据全派生零 schema）；solver.js 零改动。；**v1.23 已实施（2026-09-26）**——新增技巧教学关「数对占位」（nakedPair，adv 第 3 项，SKILLS 6→7：6×6 唯一解盘，第 4 行 idx21/23 数对 {1,2} → step1 idx20 排除 1 后填 4、step2 idx23 竖列唯一填 1，均可见状态可推导；node 构造+求解器验证唯一解）+ 掌握技巧分母语义化（新增 `mapSkillTotal()` = MAP 涉及技巧去重 6：闯关卡 sub/成长卡/分享成绩卡分母改之，数对占位不入闯关口径视觉零变化；新增 `allSkillLit()` = 全部 SKILLS 点亮数：sixArts 判定改之并扩为「全部 7 枚（适龄 4 + 进阶 3）」desc 同步）+ 分享成绩卡二维码（`drawQRTo` 复用 qrcode.js，盘面右侧 150px 编码 `shareTextForCurrent()` 题目 SD 文本 + 小字「扫码玩同款题」——扫码导入玩同款，传播闭环；不遮挡盘面/徽章/页脚）+ 家长报告「🖨️ 导出/打印」（window.print() 零依赖真 PDF；`@media print` CSS：隐藏顶栏/页脚/按钮、`.view` 只留报告卡与标题、page-break-inside: avoid、根容器解除 100vh 剪裁）；solver.js 零改动、零 schema。；**v1.24 已实施（2026-09-26）**——数对占位升入闯关口径（MAP_LEVELS 末尾追加 3-4「数对占位」10 关，末尾追加不动既有索引零迁移；mapSkillTotal 自动 → 7，闯关/成长/分享「掌握技巧 Y / 7」与六艺精通分母一致；闯关卡 sub 通关分母改动态 MAP_LEVELS.length、地图标题「10 关任你战」、mapMaster desc「全部 10 关通关」）+ 成就 15→16 枚（`perfectStreak3`「连战连捷」：last3Perfect() 取 history 尾部 3 条皆 3★，导入局天然排除，零 schema）+ 家长报告打印质检（Playwright page.pdf 实测：仅标题+三卡、中文/emoji 正常、单页 A4；修复 @media print 选择器 Bug `.topbar`→`.app-header`）；solver.js 零改动、零 schema。

---

## 1. 背景与目标

| 项 | 说明 |
|---|---|
| 现状 | v1.5：三档难度 + 规则教学 + 技巧教学关×3 + 徽章墙 + 铅笔/橡皮/撤销/提示/断局/星级/打卡 |
| v1.6 目标 | **纯本地双收益**：① 错题本/收藏本（学习闭环：错哪→重练→掌握）；② 提示带讲解（教学体系第三层：在真实盘面上"讲为什么"） |
| 后续分期 | v1.7 分享题（文本→图片打印→二维码）；v1.8 分享成绩（图片+文字→微信版增强） |
| 约束 | Chrome 61 基线、离线静态 zip、零素材零依赖零服务端；localStorage 单 key（`redtools.shudurumen.v1`）；**无竞技无排行原则**（分享为展示型，不做对比） |

---

## 2. 功能总览与分期

| 功能 | 所属 | 优先级 | 形态 |
|---|---|---|---|
| 错题本（自动记录错题 + 重练） | v1.6 | P0 | 纯本地 |
| 收藏本（手动收藏 + 移除/清空） | v1.6 | P0 | 纯本地 |
| 提示带讲解（先高亮约束来源再落子） | v1.6 | P0 | 纯本地 |
| 分享题 · 题目文本（复制/粘贴导入） | v1.7 | P1 | 纯本地 |
| 分享题 · 图片（打印友好） | v1.7 | P1 | Canvas 导出 |
| 分享题 · 二维码（扫码导入） | v1.7 评估 / v1.8 | P2 | 需内嵌 QR 编码器 |
| 分享成绩 · 图片 + 文字 | v1.8 | P1 | Canvas 导出 + execCommand |
| 分享成绩 · 微信版增强 | 远期 | P2 | 依赖微信 WebView 能力评估 |

---

## 3. 错题本（v1.6 · P0）

### 3.1 数据模型

```jsonc
store.mistakes = [            // 上限 50 条，FIFO 滚动；同盘面去重（保留最新）
  {
    "id": "m_<ts>",           // 唯一 id（时间戳）
    "ts": 1760000000000,      // 记录时间（ms）
    "level": "4",             // 难度档（4/6/9）
    "board": [1,0,3,...],     // 盘面（题目，0=空）——重练用
    "solution": [1,2,3,...],  // 终盘（重练判分 + 讲解用）
    "errors": [               // 错格记录（一次冲突填数记一条）
      { "idx": 5, "wrong": 7, "ts": ... }   // 格子下标 / 填错的值 / 时间
    ],
    "hintIdx": [3, 12],       // 使用提示的格子（提示=没独立看出，视为弱错）
    "result": "win" | "quit", // 该局最终：通关 / 中途退出
    "stars": 2                // 通关时的星级（quit 为 -1）
  }
]
```

### 3.2 触发规则（什么算"错题"）

| 事件 | 是否记 | 说明 |
|---|---|---|
| 冲突填数（错误计数 +1） | ✅ 记 | 核心错题：填错的位置 + 正确值 |
| 使用提示（自动填正确值） | ✅ 记 | 提示 = 没独立看出来，属"待复习"格（`hintIdx` 单独标注，可在列表筛选"提示过的格"） |
| 通关 0 错 0 提示 | ❌ 不记 | 无错题 |
| 中途退出 | 若已有错题 ✅ / 全对退出 ❌ | 有错题且未通关 → 保留（重练入口） |
| 教学关 | ❌ 永不记 | 教学关独立标记不计进度（沿用 v1.4 约定） |

### 3.3 去重与上限

- **去重**：同盘面（`board` 序列相等）只保留最新一条——避免连败刷屏；重练后若仍错，更新时间戳。
- **上限**：50 条 FIFO（超限移除最旧）；`loadStore` 守卫补 `mistakes: []`。
- **重练闭环**：点错题 → 以原难度/原盘面进入对局（复用 startGame 的指定盘面路径）→ 通关后若 0 错 0 提示 → 弹「🎉 这道题你已经掌握啦」并从错题本移除（掌握即清，保持列表"未掌握"语义）。

### 3.4 UI 与入口

- 难度页新增「📕 错题本」（`teach-btn` 家族，带角标显示条数，0 条时不显示入口）。
- 列表视图（仿徽章墙 `showBadgeWallView` 模式）：盘面缩略（小棋盘复用渲染）+ 难度 + 错格数/提示数 + 时间；操作：点卡片重练 / 条目内「🗑 移除」/ 底部「清空错题本」（二次确认浮层）。
- 重练对局内：**错题回看提示**——顶部小字「这格上次填错过」标注错格（浅红标记，非错误状态，不计错）。

---

## 4. 收藏本（v1.6 · P0）

### 4.1 数据模型

```jsonc
store.favorites = [           // 上限 50 条；手动管理
  { "id": "f_<ts>", "ts": ..., "level": "9", "board": [...], "solution": [...] }
]
```

### 4.2 交互

- **加入**：结算浮层新增「⭐ 收藏这局」（通关后；未通关也可收——收藏"想再练的难题"）。已收藏 → 文案变「⭐ 已收藏」。
- **列表**：难度页「⭐ 收藏本」入口 → 列表（缩略 + 难度 + 时间）；点卡片重练；条目「🗑 移除」；底部「清空收藏本」二次确认。
- 与错题本关系：**两本独立**（收藏=主动、错题=被动），同一盘面可同时存在两条；共享 `board` 序列去重互不干扰。

---

## 5. 提示带讲解（v1.6 · P0）

### 5.1 现状

提示 = `findHintCell` 找候选最少格 → 自动填正确值 + 文案「第 r 行第 c 列只能填 X」——只给答案，不讲理由。

### 5.2 升级机制（对齐开发方案 §5「题面引导 v2」：先高亮约束来源，再落子）

1. **选格**：沿用 `findHintCell`（候选最少）。
2. **判因**：新增纯函数 `explainHint(N, board, solution, idx)` → 对 hint 格，检查行/列/宫三组：各自已有数字集 ∪ 该格候选 → 判定 X 唯一性的**约束来源**：
   - 行缺失集含 X 且 X 不在该行 → 来源 = 行；
   - 列 / 宫同理；
   - 多来源 → 取最先命中的主来源（文案：「看第 r 行 / 第 c 列 / 这个宫」）+ 可列完整缺失集。
3. **交互（实施版：同步落子 + 讲解高亮，非两步动画）**：点击「提示」→ **同步落子** + 讲解文案「提示：看第 r 行/第 c 列/这个宫：已经有 a、b、c，缺 d——这格只能填 d」+ 约束来源单元高亮（`.cell-inner.peer` 浅蓝，**3s 后自动清除，下一次用户操作亦清除**）。实施时评估过"先高亮再落子"的两步动画，为避免 350ms 延迟落子与用户操作/断局快照的竞态，改为同步落子 + 讲解高亮（讲解价值 = 文案 + 高亮同时呈现）；后续若需纯两步引导可再评估。
4. **落子时机**：与教学关一致——讲解后再填，避免"提示即答案"的无效性。
5. **限流**：按难度档限提示次数（对齐开发方案 §2 input 演进）：4×4 不限制 / 6×6 每局 ≤3 / 9×9 每局 ≤2（超限提示按钮置灰 + 文案「先试试自己找」）。

### 5.3 与教学体系呼应

- 提示讲解的文案口径与技巧教学关一致（"缺哪个就填哪个"）；讲解命中的约束来源若对应已学技巧（单宫/行列/区块排除），文案可点技巧名（如「这步用到了你学过的『行列排除』」）——联动徽章墙（v1.5 已存 `store.skills`）。

---

## 6. 分享题（v1.7 · P1 / 二维码 P2）

### 6.1 题目文本（先行）

- **导出格式**（复制文本）：
  ```
  数独思维 · 4×4 · 已知 10 格
  SD4:1,0,3,4,3,4,0,2,2,1,4,3,4,3,2,1
  （空格填 0；复制后在数独思维「📥 导入题目」粘贴）
  ```
- **导入入口**：难度页「📥 导入题目」→ 粘贴文本框 → 校验（长度=N²、值域 0..N、唯一解可解）→ 进入对局（标记「他人分享」）。
- 校验失败温和提示（对齐现有提示文案风格）；导入盘不计入最佳成绩（防刷榜，本地工具则防误记）。

### 6.2 题目图片（打印友好，P1）

- Canvas 绘制：黑白 + 大格（A4 打印友好）+ 标题「数独思维 · 第 N 题」+ 题面（空答案）；**不渲染答案**（学生版）。
- 可选「答案版」切换（家长判分用）。
- 弹层「长按保存图片 / 打印」，复用贪吃蛇 share 弹层模式。

### 6.3 二维码（P2 · 需决策）

- **现状约束**：零依赖、Chrome 61、离线——仓库无现成 QR 实现；需内嵌纯 JS QR 编码器（约 4–8KB，ES2017 可用）。
- **决策点**：① 内嵌编码器（内容=6.1 紧凑格式，扫码→微信识别→文本→导入）；② 或暂缓，先文本+图片。
- **建议**：v1.7 先文本+图片交付；二维码随「微信版增强」一期评估（二维码的消费场景在微信生态，离线 zip 场景价值低）。

---

## 7. 分享成绩（v1.8 · P1 / 微信增强 P2）

### 7.1 图片卡（复用贪吃蛇/24点 share.js 模式）

- 离屏 canvas 1080×1920：标题「数独思维」+ 难度/盘面 + **本局盘面完成缩略图**（小棋盘绘制终盘）+ 星级 + 用时 + 错误/提示数 + 已点亮徽章数 + 日期 + 底部「龙爸乐学 · 数独思维」。
- 弹层「📸 长按保存图片 · 分享到小红书/朋友圈」。

### 7.2 文字分享（先图片+文字，微信版增强二期）

```
🎉 我在「数独思维」挑战 9×9 困难成功！
⭐⭐⭐ 3 星 · 用时 8分32秒 · 0 错 0 提示
已点亮 3 枚技巧徽章 🏆
来一起破案吧～#数独 #小学生逻辑 #龙爸乐学
```

- 复制机制：隐藏 textarea + `execCommand('copy')` + 失败兜底（toast 展示全文供手动复制）——与贪吃蛇一致。
- **无竞技红线**：文案只写自身成绩，**不写**"超过 X% 玩家/打败了谁"（对齐标准答案原则/无排行）。

### 7.3 微信版增强（P2 · 远期）

- 微信内置 WebView 能力评估：分享卡片长按识别 / JS-SDK（需域名+鉴权，与离线 zip 形态冲突）→ 结论大概率维持"图片+文字"为终态，仅做图片尺寸/文案适配。

---

## 8. 存储 schema 扩展（v1.6）

```jsonc
// 现有 store（不变）
// v1.6 新增（defaultStore 补默认 + loadStore 守卫补全，均零迁移——同 v1.5 skills 模式）
"mistakes": [],    // §3.1
"favorites": [],   // §4.1
// 提示限流计数（对局内存态，不入库；断局恢复快照含 hints 计数）
```

> `store.version` 仍为 1，不 bump（schema 追加字段无需迁移，守卫补一行即可）。

---

## 9. UI 落点与路由汇总

| 视图/按钮 | 位置 | 动作 |
|---|---|---|
| 📕 错题本（有角标条数） | 难度页（徽章墙入口下方） | → showMistakeView |
| ⭐ 收藏本 | 难度页（错题本旁） | → showFavoriteView |
| 📥 导入题目 | 难度页（收藏本旁） | → 粘贴弹层 → 校验 → startGame(指定盘面) |
| ⭐ 收藏这局 | 结算浮层 | push favorites |
| 📤 分享这题 | 难度页结算浮层/对局内工具条 | → 文本/图片弹层（v1.7） |
| 📤 分享成绩 | 结算浮层 | → 图片卡/文案弹层（v1.8） |
| 提示讲解两步动画 | 对局内「提示」按钮 | explainHint + 高亮 → 落子 |

路由约定：列表视图仿 `showBadgeWallView`（renderHeader + 卡片 + ← 返回）；**返回目标硬编码难度页**（无上级栈，沿用现有约定）；所有新视图不启动计时器、不触碰 store.best/checkin。

---

## 10. 约束与风险

| 风险 | 等级 | 对策 |
|---|---|---|
| QR 编码器依赖/体积 | 中 | 分期：v1.7 文本+图片；二维码随微信版评估（§6.3） |
| localStorage 体积（图片不走库） | 低 | 只存 JSON 记录；canvas 即时生成不落库；上限 50 条 |
| execCommand('copy') 兼容 | 低 | 已有兜底路径（贪吃蛇验证过 Chrome 61） |
| 错题记录隐私（儿童数据最小化） | 低 | 纯本地不联网；无账号无上报 |
| 提示讲解与"自己思考"矛盾 | 中 | 限流 + 讲解先行（先看到原因，再落子），不直接给答案前先引导 |
| 导入盘刷最佳成绩 | 低 | 导入盘不计入 best/checkin（标记来源） |
| 无竞技原则 | 低 | 分享只写自身成绩，无对比无排行（§7.2 红线） |

---

## 11. 测试与验收

| 项 | 验收口径 |
|---|---|
| 冒烟 `smoke_shudu_mistake.py`（v1.6 新增） | 错题自动记录（冲突填数后错题本 +1）/ 提示记录 hintIdx / 重练闭环（0 错 0 提示通关后自动移除）/ 移除/清空（二次确认）/ 收藏加入-移除-清空 / 导入校验（合法进入、非法温和拒绝）/ 全程无 JS 报错 |
| 冒烟 `smoke_shudu_hint.py`（v1.6 新增） | 提示两步动画（先高亮约束来源 peer 4 格 → 再落子）/ 文案含来源行/列/宫 / 6×9 限流（第 N+1 次按钮置灰）/ 断局恢复后限流计数保留 |
| 回归 | skill/badge/teach/pen/resume/main 全量 ALL PASS（版本断言 → v1.6） |
| 视觉质检（多模态 + 计算样式） | 错题本/收藏本列表布局、提示高亮来源可见、结算浮层新按钮 |
| 构建/审计 | zip 包体 ≤0.05 MiB、audit 0 warning |

---

## 12. 依赖模式复用清单

| 模式 | 来源 | 复用点 |
|---|---|---|
| 分享弹层 + canvas 打卡卡 + toDataURL | `贪吃蛇/src/assets/share.js`、`24点/main.js` | 分享成绩/分享题图片 |
| 文案复制（textarea + execCommand + toast 兜底） | 同上 | 分享文字/题目文本 |
| 徽章墙列表视图 | `数独思维` v1.5 `showBadgeWallView` | 错题本/收藏本列表 |
| 指定盘面开局 | v1.5 教学关 `openSkill`（board 注入）+ 断局恢复 `resumeGame` | 错题/收藏/导入重练 |
| 教具高亮（`.cell-inner.peer`） | v1.5 两层渲染修复 | 提示讲解约束来源高亮 |
| 限流模式 | 开发方案 §2（提示次数按难度档） | 6×6/9×9 提示限流 |

