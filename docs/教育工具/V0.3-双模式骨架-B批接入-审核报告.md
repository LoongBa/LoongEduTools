# 双模式骨架 B 批批量接入 — 审核报告（V0.3）

> 审核对象：提交 `a77b250` feat(双模式骨架): B 批批量接入 38 款存量工具 lx-shared.js + tools.py 登记 modes/free_units
> 审核方式：oracle 后台实施后审核（session `ses_f3e48cee5ffetUhAdlXAj12oiy`，2026-09-21）+ 本轮修复验证
> 关联方案：`docs/教育工具/V0.2-双模式骨架-开发方案.md`（oracle 审核通过）｜设计要求书：`docs/教育工具/教育工具在线版设计要求.md` §六 N 值表

## 1. 审核范围

| 维度 | 对象 |
|---|---|
| 代码 | `tools.py` 全量 44 款 ToolConfig、`build_framework.py` 全量 974 行、`build_all.py` 全量 |
| 产物 | 5 款代表 index.html script 顺序实测（英语点读/点读陪练/24点/舒尔特/数学口算）；39 款 main.js LX_SHARED 引用全量扫描；content.js 全量 glob |
| 行为 | N 值表逐项核对、book 分支处理、shared_js 默认值、经典游戏跳过、合规红线、注入顺序、遗漏核查 |

## 2. 结论：✅ 通过（附 7 项待补完，无阻塞性问题）

B 批「骨架接入」达成：**script 注入 40 款 + tools.py 登记 39 款 + 双模式构建管线 + online 合规隔离** 全部正确，0 JS 错误，审计 PASS。
属「**接入完成、逻辑未迁移**」形态——38 款 main.js 不消费 LX_SHARED、无 content.js，free_units 切分空转。逻辑迁移留 V0.4。

## 3. 验证证据

- **7 项审核逐项**：N 值表 9/11 类完全一致（推箱子/蜡笔物理为关卡制语义合理但表义偏离 → 本轮已补文档）；free_units=None 语义正确（book 分支不读该字段，无异常）；注入顺序 5 款全合规（data.js → lx-shared.js → [adapters/core] → main.js）；shared_js 默认 True 触发合并正确（38 款无需显式登记）；经典游戏 4 款不报错但产生死文件（→ 本轮已修）；无遗漏（44 款 = 接入 40 + 跳过 4）；online 不入 publish 红线守住（build_framework.py L914-917）。
- **双包构建**：offline 44 成功 + online 44 成功（online 仅 dist/ 部署目录）。
- **Playwright 回归**：14 款代表工具 14/14 PASS，lx-shared 0.1 就绪，0 JS 错误。
- **审计**：3 款抽查 PASS（6-7 files 0 warning）。

## 4. 变更摘要（V0.3 含本轮修复）

1. B 批批量接入（`a77b250`）：38 款 index.html 注入 `./_shared/lx-shared.js` + tools.py 登记 `modes=["offline","online"]` + `free_units`。
2. **P6 修复**：设计要求书 §六 N 值表补注关卡制工具切分语义（推箱子前 20 关/蜡笔物理前 5 关/华容道前 2 关、扩展款、book 源 None 语义）。
3. **P5 修复**：经典游戏 4 款（2048/俄罗斯方块/扫雷/贪吃蛇）显式 `shared_js=False`，消除打包死文件（验证：2048.zip 5 files 无 _shared/，0.01 MiB）。

## 5. 版本建议（oracle 结论）

- 本轮 B 批属 V0.2 骨架的「接入完成」阶段 → 建议里程碑 **V0.3**。
- 待 main.js 迁移 + content.js 结构化完成后再升 **V0.4**（B 批逻辑迁移）。

## 6. 遗留问题（按优先级，待后续迭代）

| # | 严重度 | 问题 | 建议 |
|---|---|---|---|
| P1 | 🟡 中 | 38 款 main.js 未迁移 LX_SHARED（仅点读陪练 core/main.js 引用 8 处）——lx-shared.js 为「加载未消费」死文件 | V0.4 按 V0.2 §2.1 补 main.js 迁移（loadStore/saveStore/checkin → LX_SHARED.*），或文档化「B 批只接入骨架」 |
| P2 | 🟡 中 | 38 款无 content.js，free_units 切分空转（content 恒为 `[]`） | V0.4 为需切分工具建 content.js（数学口算 POINTS 数组化、词汇表 books 数组化等） |
| P3 | 🟡 中 | 数学口算 A 批 main.js 残留 loadStore/saveStore/calcStreak（L75/L83/L734），违反 V0.2 §5.3 验收① `grep -c == 0` | V0.4 补完 main.js 迁移，或修订验收标准 |
| P4 | 🟢 轻 | 英语点读 book 分支 `write_data_js` 不写 meta.mode/free_units/api_version（违反 V0.2 §2.3 契约；lx-shared-core 防御性默认 offline，main.js 不消费故无功能影响） | 待 build_framework.py 并行线（英语点读 v1.3.1）稳定后补 meta 字段 |
| P6 | 🟢 轻 | 推箱子=20/蜡笔物理=5 关卡制表义与 §六「前 2 档」不一致 | ✅ 本轮已补文档 §六「切分语义」说明 |
| P5 | 🟢 轻 | 经典游戏 shared_js 默认 True 死文件 + build_all 无 modes 守卫（`--mode online` 会构建经典游戏 online 包） | ✅ 本轮已修 shared_js=False；modes 守卫留待 build_all 并行线稳定后加（`if args.mode in cfg.modes`） |

## 7. 审核人

- Oracle（架构审核）——实施后审核通过（session `ses_f3e48cee5ffetUhAdlXAj12oiy`）
- 主会话（Sisyphus）——P5/P6 修复 + 本报告编写