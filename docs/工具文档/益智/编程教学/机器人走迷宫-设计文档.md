# 机器人走迷宫 — 设计文档

> 系列：益智 · 编程教学（子系列第 1 款，D1 原型）
> 版本：v1.2（2026-09-22 交付；v1.1 = 区段扩展 + 循环指令；v1.2 = 关卡全量 155 关）
> 状态：✅ 已实现（审计 PASS / 执行器单测通过 / 双模式接入）
> 定位：编程教学产品线原型——把「推迟」的推箱子玩法转型为**有标准答案的编程练习**
> 依据：`docs/教育工具/编程教学产品线-立项评估.md`（D1）

## 1. 定位与玩法

**一句话**：迷宫盘面（推箱子关卡），孩子用**指令积木**编排机器人的移动序列
（↑ 前进 / ↰ 左转 / ↱ 右转），点「▶ 执行」后机器人按序列移动（前方有箱子则推箱）。
所有箱子到目标点 = 答对。

**编程教学本质**（与推箱子游戏的根本区别）：
- 推箱子：直接方向键实时控制（表现性玩法）；
- 机器人走迷宫：**先编排指令序列 → 一次性执行 → 观察结果 → 调试修正**（顺序编程思维，
  像 Scratch 的指令序列阶段）。

## 2. 有标准答案论证（合规关键）

| 判定维度 | 机制 |
|---|---|
| 对/错 | 指令序列执行后的盘面 = 所有箱子在目标（`isWon` 程序判定，非表现性分数） |
| 星级 | 指令数 vs 关卡最少推动数（`bestPushes` b 值，≤1.5× = 3★，程序可算） |
| 学习反馈 | 执行失败显示「还有箱子没到目标点，调整指令再试」→ 像调试器定位错误序列 |

> 无生存/分数/时间竞争——纯「编排-执行-验证」循环，编程练习不是游戏。

## 3. 关卡与难度

| 难度 | 关卡区段 | 星级标准 |
|---|---|---|
| 简单 | 第 1-30 关 | 步数 ≤ 1.5×b = 3★ / ≤ 2.5×b = 2★ / 其余 1★ |
| 普通 | 第 31-80 关 | 同左 |
| 挑战 | 第 81-155 关 | 同左 |

- 关卡数据：复用推箱子 `levels.js`（Microban 155 关全量，v1.2 起全部开放）；
  61-155 关含 19 关 b=null（星级兜底 2★，finishLevel 已有分支）；
- 每关 b 值（最少推动数）为星级基准（levels.js 预计算）。

## 4. 核心机制

- **解析**：XSB 关卡 → 位集合（walls/goals/boxes/player），复用推箱子解析；
- **执行器**（execStep）：逐条指令——left/right 改朝向（face ±90°），fwd 按朝向移动
  （前方有箱子则推箱，撞墙/越界忽略）；
- **判定**：指令执行完 → `isWon(boxes, goals)`（所有箱子在目标）；
- **指令序列**：可追加/删除（✕ 角标）/清空/重置盘面；
- **循环指令（v1.1）**：指令 chip 单击循环次数（×1→×2→×3→×4，编程「重复」启蒙）；
  执行器对 rep>1 指令重复执行 rep 次（星级基准 = 实际步数 = 循环展开后）；
  ✕ 角标删除指令。

## 5. 数据与存储

- `LX_SHARED.storage`：`configure({toolName: 'jiqirenzoumi'})` → 键前缀 `redtools.jiqirenzoumi.v1`；
  结构 `{version, best:{easy,normal,hard}, recent, checkin, history}`；
- `calcStreak(dates)` → `LX_SHARED.progress.streak(dates)`。

## 6. 合规

- 有标准答案（目标盘面判定）→ 安全练习区（编程教学，非游戏）；
- 无排行榜/无跨用户对比 → 个人纵向（星级/打卡/最佳）；
- UI 文案「指令/执行/前进/转向/完成练习」编程练习用语；
- 零图片素材（SVG 程序化盘面），包体 0.02 MiB。

## 7. 技术要点

- Chrome 61 基线（ES2017 经典脚本、CSS 无 gap/aspect-ratio/clamp）；
- 双模式脚本顺序 `data.js → lx-shared.js → levels.js → main.js`；
- 构建：`python build_all.py --tool 机器人走迷宫`；
- 复用：levels.js（Microban 155 关）+ XSB 解析 + 推箱判定逻辑。

## 8. 验证记录

| 项 | 结果 |
|---|---|
| 构建 | offline zip 0.02 MiB + online 部署目录成功；全量 49 工具回归通过 |
| 审计 | PASS（7 files 0 warning） |
| 合规自查 | 无禁语/无 ES2017 越界/无 localStorage 直调/脚本顺序正确 |
| 执行器单测 | 初始解析 ✓ / 前进移动 ✓ / 重置恢复 ✓ / 转向+前进方向正确 ✓ / **循环指令（×3 chip 切换 + rep 执行 + _loopLeft 清理 + 0 JS 错误）✓** |
| 区段扩展 | 简单 1-15 / 普通 16-40（起点第 16 关）/ 挑战 41-60（起点第 41 关）✓ |
| 判定链路 | 指令编排 → 执行 → 盘面更新 → 判定（单测确认 execDone/isWon）|
## 9. 概念教学路线与条件指令评估（v1.2）

**教学路线**：顺序（指令序列）→ 循环（v1.1 chip ×N）→ 条件（v1.3 评估中）。

**条件指令（if/else）评估**：
- **暂缓完整 if/else**：对目标用户（preK-K1-3，3-8 岁）认知负担过大（需理解「条件求值 + 分支跳转」），
  不符合指导原则「低龄友好」；且执行器需引入运行时分支架构（条件指令在静态序列中动态求值）。
- **v1.3 候选「避障守卫」**（最小条件形式）：`🚧 前方有障碍则跳过`（单分支守卫，执行器改造小）——
  教学价值：让机器人「遇到墙自动绕开」，比完整 if/else 更贴近低龄直觉；
- **触发条件**：目标用户年龄上移（K4-6）或用户明确要求时再实现；当前 155 关 + 循环指令已覆盖教学完整度。
