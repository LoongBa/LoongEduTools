# 机器人走迷宫 V1.10 — 审核报告

> 版本：v1.10（自定义指令块 = 抽象/函数概念启蒙）
> 审核范围：实施结果代码级复核（Oracle 后台评审）
> 前置：`V1.10-开发方案.md`（v0.2 修订版，Oracle 复核通过）
> 审核日期：2026-09-25

---

## 1. 审核结论

**✅ 通过**（0 阻断 / 1 重要已修复 / 3 建议不入轮）

Oracle 实施后代码级复核：方案 v0.2 全部 8 点落地，7 项关键逻辑（renderSeq 闭包 / execCall 共享引用 / 块编辑返回流程 / totalSteps seenBlockIds / 孤儿三路径一致性 / 清除数据残留 / Chrome 61 兼容）全部复核通过。
**唯一 I 级缺陷（editBlockBody 浅拷贝污染）在实施阶段由主会话自主发现并先行修复**，复核确认修复方向正确、回归通过。

## 2. 审核范围（Oracle 逐项核查）

| # | 检查点 | 实现位置 | 结论 |
|---|---|---|---|
| 1 | customBlocks 存储（键无 .v1. 段 / MAX_BLOCKS / MAX_BODY / 5+1 函数） | main.js L79-124 | ✅ |
| 2 | 块体仅基础指令（决策 B，无 if/loop/call） | L464 BLOCK_BODY_CMDS | ✅ |
| 3 | 块编辑器独立视图 + I-4 修订（不误锁）+ 游戏返回计时衔接 | L466-595 | ✅ |
| 4 | 引用链路（cmd-bar 8 按钮 / body 预览 N-2 / call chip I-3 / 孤儿灰显） | L1097-1278 | ✅ |
| 5 | 执行器（execStep call 分支 / execCall I-2 防递归 / B1 / I-new） | L1631-1730 | ✅ |
| 6 | totalSteps I-1（call 计 1 + seenBlockIds body 一次） | L1850-1873 | ✅ |
| 7 | 清除数据覆盖（remove('customBlocks') + N-6 语义） | L996-998 | ✅ |
| 8 | setCmdLocked 选择器扩展 | L1842 | ✅ |

## 3. 发现与处置

| 级别 | 编号 | 问题 | 处置 |
|---|---|---|---|
| **B** | — | 无 | — |
| **I** | I-1 | **块编辑浅拷贝污染**：`viewBlockEdit` 用 `def.body.slice()` 浅拷贝，块体指令对象与块库共享引用——点击 chip 改 rep/steps 后「取消」，内存 customBlocks 已被污染（storage 未同步），直到 reload 才恢复 | **已修复**：改为逐元素深拷贝（map 手写克隆 {id, rep?, steps?}，Chrome 61 兼容）；污染防护专项 12/12 PASS（编辑改 rep→取消→块库不变 / 编辑→保存→更新生效 / 游戏内编辑→保存→引用即时生效） |
| N | N-1 | totalSteps seenBlockIds 函数内声明：call 在 if/loop 分支 + 顶层同块时 body 计两次（保守方向，design 接受） | 不入轮，backlog |
| N | N-2 | totalSteps 无递归守卫（仅 debug 直调可触发，生产不可达） | 不入轮，backlog |
| N | N-3 | 块选择面板「管理块」从游戏进入后无返回游戏路径（会回首页丢指令序列） | 不入轮，backlog（UX 取舍） |

## 4. 验证证据

| 项 | 结果 |
|---|---|
| Node totalSteps 单元 | ✅ 13/13（call 计 1+body 一次 / 多 call 同块 body 仅计一次 / 带 rep 块体 / 孤儿计 1 / 既有口径回归 / 激励 3 call=11 vs 直接 24） |
| Playwright 冒烟（file:// + http://） | ✅ 60/60 + 0 JS 错误（块卡/块库/新建/校验/面板/引用/编辑/孤儿/存储键/清除覆盖/回归） |
| Playwright 执行专项（双协议） | ✅ 30/30 + 0 JS 错误（块引用真实通关 2★/重复引用/编辑即时生效/删除兜底/清除数据） |
| **污染防护专项（双协议，I-1 修复验证）** | ✅ 12/12 + 0 JS 错误（取消不污染 / 保存更新 / 游戏内编辑保存即时生效） |
| 回归（v1.8 + v1.9） | ✅ smoke_v18 30/30 + smoke_v19 38/38（既有 7 指令/循环/嵌套/关卡编辑器无破坏） |
| 视觉质检（7 截图） | ✅ 7/7（首页块卡紫渐变/块库列表/块编辑器/块选择面板+body 预览/call chip 紫/孤儿灰显/执行反馈） |
| 构建 + 审计 | ✅ build_all offline/online 双模式成功；audit PASS（15 files 0 warning） |
| Chrome 61 基线 | ✅ 全 var/function、无现代 API 越界（Oracle 复核确认） |

## 5. 变更摘要（对照 v1.9）

| 文件 | 变更 |
|---|---|
| `series/益智/编程教学/机器人走迷宫/src/assets/main.js` | +customBlocks 存储层（MAX_BLOCKS=8/MAX_BODY=8/名称唯一）/ +CMD_CALL / +viewBlockList·viewBlockEdit·viewBlockEditFromGame·exitBlockEdit（编辑来源记忆 + 计时衔接）/ +renderBlockPicker（N-2 body 预览）/ renderGame cmd-bar 8 按钮 / renderSeq call chip（I-3 name 查库最新·孤儿灰显·点击进编辑）/ addCmd call 分支 / execStep call 分支 + execCall（I-2 防递归 + B1 + I-new）/ totalSteps call 分支（I-1 seenBlockIds）/ viewParent 清除覆盖 / setCmdLocked 选择器扩展 / M 导出；**I-1 深拷贝修复（viewBlockEdit 逐元素克隆）** |
| `series/益智/编程教学/机器人走迷宫/src/assets/style.css` | +紫色主题（.cmd-add-call / .cmd-chip.call / .call.deleted / .block-picker* / .block-card） |
| `tools.py` | version 1.9 → 1.10 |
| 文档 | 设计文档 §2.9/§3/§4/§6/§7/§9 + 使用文档 §3.6/FAQ + 矩阵 + 看板 + 系列 README + RedTools README |

## 6. 版本建议

| 项 | 建议 |
|---|---|
| 版本 | **v1.10**（minor：新功能自定义指令块——抽象/函数概念） |
| Tag | `编程教学-v1.10`（发布前征求用户确认） |
| 遗留 | N-3（管理块返回游戏路径）记 backlog；Chrome 61/真机未实测（全工具统一遗留） |