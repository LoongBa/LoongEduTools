# 机器人走迷宫 V1.16 — 审核报告

> 版本：v1.16（块内控制流嵌套：if 分支内嵌 loop——条件×循环双维组合，块能力强化收官）
> 审核依据：AGENTS.md §6A（开发方案 → Oracle 审核 → 实施 → 验证 → 实施后审核 → 文档 → 提交发布）
> 方案：`V1.16-开发方案.md`（v0.1 → Oracle 审 2B/2I/10N → v0.2 修订 → Oracle 复核通过）
> 更新：2026-09-25

---

## 1. 审核范围

| 项 | 说明 |
|---|---|
| 功能 | 块内 if 分支内嵌 loop（条件×循环嵌套）：分支添加行放开 🔁 循环，分支内 loop 编辑态 + 返回/删除回 if 双栏（来源记忆）、嵌套校验放宽（分支顶层禁 if/call、分支内 loop.body 单层） |
| 代码 | `series/益智/编程教学/机器人走迷宫/src/assets/main.js`（v1.15 约 2610 行 → v1.16 约 2652 行） |
| 文档 | 设计文档 §2.14/§7/§9 + 使用文档 §3.7 + 矩阵 + 看板 + README + tools.py（本轮同步） |
| 约束 | Chrome 61 / ES2017 / 无外部资源 / localStorage / 双协议冒烟 / 0 新关 / 存量块零迁移 |

---

## 2. Oracle 审核纪要

### 2.1 方案审核（v0.1，后台 oracle bg_cc8476d6）

| 级别 | 编号 | 内容 | 结论 |
|---|---|---|---|
| **B** | B1 | 返回按钮逻辑漏清 `editBlockLoopTarget`（from 非 null 分支未清 → renderBlockEdit L587 先查 loopTarget 再进 loop 编辑态 → 核心功能失效） | 已修（v0.2：始终清 + 条件恢复） |
| **B** | B2 | 删除按钮变量名错（`editBlockIfTarget.then` → 此时为 null 会 TypeError，应用 `editBlockLoopFrom`）+ 缺 from 恢复 + 缺 hasEditBodyParam 重派生 | 已修（v0.2：完整 6 步） |
| I | I1 | `hasParamInBody`/`toggleStepsInArr`/`toggleBlockHasParam`/`hasStepsInArr` 不递归 if 分支（v1.15 既有缺口，v1.16 放大）——执行无影响（scanParamSteps 递归正确），toggle UI 可能不一致 | 归遗留（方案 §2.5 注明，本版不修） |
| I | I2 | if chip 进 if 编辑态应强制 `editBlockLoopFrom = null`（防御） | 已采纳（L865 + viewBlockEdit 重置链） |
| N | N1-N10 | 零改动确认（scanParamSteps/cloneBlockBody/execCall 共享引用/hasForbiddenNested 调整/MAX_BODY/pickerPreviewSpan/M 导出/title/CSS）+ 注释同步 + blockHasLoop 限制 | 已确认 |

### 2.2 v0.2 复核（后台 oracle bg_5dab4e60）

**复核通过，可实施**——v0.2 正确闭合 2B/2I/10N 全部意见：
- B1 返回：始终清 loopTarget + 条件恢复 ifTarget ✅
- B2 删除：editBlockLoopFrom 双路径定位（by ===）+ from 恢复 + hasEditBodyParam 重派生 ✅
- 进入顺序：捕获来源在清空前 ✅
- hasForbiddenNested：分支顶层禁 if/call（loop 放开）+ loop.body 递归校验，逐场景验证无漏网 ✅

### 2.3 实施后复核（后台 oracle bg_c191e416）

**复核通过**（1 项 N 级 cosmetic 遗漏）：

| 级别 | 编号 | 内容 | 处置 |
|---|---|---|---|
| **N** | N1 | L1042 保存拒绝文案未随「分支放开 loop」更新（仍称"分支里不能放循环"） | **已修**：改为「循环里不能放循环/条件/块引用，分支里不能放条件/块引用」；回归断言同步更新后全绿 |

- 核查结论：实施忠实落地方案 v0.2 全部核心改动（B1/B2/I2/hasForbiddenNested/常量/注释）；零改动函数（scanParamSteps/cloneBlockBody/execCall/execIf/hasCallInBlock/resetLoopLeft/totalSteps/pickerPreviewSpan）确认无意外修改；无新功能缺陷；editBlockLoopFrom 无悬空路径；三态往返（块体→if→loop→返回/删除→if→块体）状态变量清零一致。

---

## 3. 验证证据

### 3.1 单元测试（Node 无 DOM，`v16-verify/unit_v116.js`）— 31/31 PASS

| 组 | 断言数 | 覆盖 |
|---|---|---|
| scanParamSteps 分支内 loop.body 参数 | 4 | 分支内循环参数化 / 双参槽 / 无参数 → blockParamCount 派生 |
| cloneBlockBody 分支内 loop 深拷贝 | 4 | 独立副本 / 固定值保护 / 编辑深拷贝结构 / 改副本不改库 |
| totalSteps 分支内 loop 星级 | 3 | 单 call = 5 / 双 call = 6 / 参数×条件×循环 = 4 |
| hasForbiddenNested 放宽语义 | 8 | 分支顶层 loop 合法 / 分支内 if·call 拒绝 / loop.body 嵌 if·loop·call 拒绝 / v1.15 存量零回归 |
| hasIllegalNested 整链 | 6 | 合法分支 loop / 非法（if.then 嵌 call·if / loop.body 嵌 if）/ hasCallInBlock 兜底 |
| B2 删除双路径模拟 | 6 | then·else 分支定位 / 顶层回退 / 多分支引用唯一 |

### 3.2 冒烟测试（Playwright file:// + http:// 双协议，--no-proxy-server）— 52/52 PASS

| 项 | 断言要点 |
|---|---|
| 分支添加行 6 按钮 | 基础+steps+🔁循环（无 if/call——入口隔离） |
| 分支内 loop chip | `.loopb` 类复用 + `🔁×2` 显示 |
| 分支内 loop 编辑态 | 标题「🔁 循环（块内）」+ body 添加 |
| **B1 显式** | 返回 → 双栏标题「分支编辑」+ then/else 保留 + loop chip 位置未丢 |
| **B2 显式** | 删除 → loop 从分支消失 + 回 if 双栏 |
| 保存校验 | 分支内 loop 合法保存成功；分支内 if 拒绝；分支内 loop.body 嵌 if 拒绝 |
| 顶层 loop 回归 | from=null 路径回块体（v1.14 行为不变） |
| 条件×循环执行 | execIf→execLoop 链 0 JS 错误 |
| 参数×循环注入 | call 传参执行 + 块库 steps:null 保持（递归副本） |
| 共享引用无污染 | 双调用后块库分支 loop 未变 |
| 存量回归 | 固定块/单参块预览 + 存储键无 .v1. 段 |

### 3.3 全量回归 — 全绿

| 版本 | 通过 | 说明 |
|---|---|---|
| v1.15 冒烟 | 46/46 | 断言随 v1.16 更新 2 处（分支添加行 6 按钮 / 保存文案） |
| v1.14 冒烟 | 42/42 | 断言随 v1.16 更新 1 处（保存文案） |
| v1.13 冒烟 | 54/54 | 存根 54/54（文案断言已兼容） |
| v1.12 冒烟 | 28/28 | 存根 28/28 |
| v1.11 冒烟 | 54/54 | 存根 54/54 |
| v1.10 冒烟 | 60/60 | 存根 60/60 |
| v1.9 冒烟 | 38/38 | 存根 38/38 |
| v1.8 冒烟 | 30/30 | 存根 30/30 |
| v1.19 保存拦截 | PASS | 编辑器保存校验路径 |

### 3.4 审计 + Chrome 61 + 视觉

| 项 | 结果 |
|---|---|
| audit_artifact（dist offline） | **PASS：7 files 0 warning** |
| Chrome 61 兼容扫描 | ES2018+ 仅注释中 `??`（L1656 params[slot]??…，v1.13 已知确认）✅ |
| 视觉质检（looker 4 场景） | **4/4 PASS**（if 双栏 + loop chip / 循环编辑态 / 返回双栏一致 / 面板预览 🔁 标记）；else 分支「空槽」疑点 = looker 箭头字符 OCR 局限（冒烟 DOM 断言已实证 `↰`/`↑` 正常渲染） |

---

## 4. 变更摘要

| 变更 | 说明 |
|---|---|
| **BLOCK_BODY_BRANCH_CMDS**（新增） | 分支添加行 = 基础 + 参数化 steps + loop（不含 if/call）；loop.body 添加行仍用 BLOCK_BODY_LOOP_CMDS（单层保持） |
| **editBlockLoopFrom**（新增状态） | loop 编辑态来源记忆：进入捕获（清空前）、B1 返回（始终清 loopTarget + 条件恢复 ifTarget）、B2 删除（双路径定位 + from 恢复 + hasEditBodyParam 重派生）——三态往返（块体→if→loop）闭环 |
| **hasForbiddenNested 语义调整** | 分支顶层禁 if/call（loop 放开）；分支内 loop.body 递归校验（禁 loop/if/call——双层嵌套封顶）；双重保险（doSaveBlock + hasCallInBlock） |
| **I2 重置** | if chip 进入强制 editBlockLoopFrom=null + viewBlockEdit 重置链 |
| **零改动确认** | scanParamSteps/cloneBlockBody/execCall/execIf/hasCallInBlock/resetLoopLeft/totalSteps/pickerPreviewSpan——递归已覆盖，v1.16 无意外改动 |
| **注释/文案** | L851（块体+分支层可进）/L1805（分支可含 loop 递归预览）/blockHasLoop N10 限制/addBlockBodyCmd if 注释/doSaveBlock 文案（N1 修正） |
| **N1（Oracle N 级）** | 保存拒绝文案随分支放开 loop 更新（修正 + 回归断言同步） |

---

## 5. 版本与发布建议

| 项 | 建议 |
|---|---|
| 版本 | **v1.16**（minor：块内控制流嵌套——条件 × 循环双维组合；设计文档 §9 块能力强化收官） |
| Tag | `编程教学-v1.16`（发布前征求用户确认） |
| 前置 | v1.15（块体含 if，已发布 tag `编程教学-v1.15`） |

---

## 6. 遗留

| 项 | 说明 |
|---|---|
| I1（Oracle I 级，既有缺口） | `hasParamInBody`/`toggleStepsInArr`/`toggleBlockHasParam`/`hasStepsInArr` 不递归 if 分支——toggle UI 状态可能与执行派生不一致（执行无影响）；归后续版本对齐 scanParamSteps |
| N10（已知限制） | `blockHasLoop` 仅扫顶层不递归 if 分支（无调用方，文档/教学用） |
| 真机 WebView 实测 | 全工具统一遗留（无 Android 8.1 真机环境，如实标注「未实测」） |
| 后续候选 | 设计文档 §9 块能力候选已全部落地（顺序/循环/条件/调试/分支/参数化/循环块/嵌套循环/抽象/块参数/参数贯穿/混合态多参/块体 loop/块体 if/块内控制流嵌套）——下一阶段候选：在线服务端（B4，远期） |