# 机器人走迷宫 V1.15 — 审核报告

> 版本：v1.15（块体含 if：条件 × 抽象组合——条件抽象渐进）
> 审核范围：方案评审 + 实施结果复核
> 前置：`V1.15-开发方案.md`（Oracle v0.1 审：4B 描述缺口 + I1-I4 + N1 → v0.2 修订 → 复核通过）
> 审核日期：2026-09-25

---

## 1. 审核结论

**✅ 通过（Oracle 方案审核 ×2 轮 + 实施中自发现 hasIllegalNested 真 bug + 全量自动化验证）**

Oracle 对 v0.1 审核：**B1（泛化改双状态）+ B2（execIf 展开方式验证）+ B3（clone 调用关系）+ B4（ifDepth 交互）** + I1-I4 + N1；
v0.2 修订后 Oracle 复核通过（双状态闭环 / splice.apply 等价安全 / 统一入口正确 / 共用计数器正确；唯一实施注意 resolveStep null 守卫——已由 paramCmd 三态短路天然满足）。
实施严格按 v0.2 落地，**实施中自发现并修正 1 个真 bug**（hasIllegalNested 递归漏检 if 分支嵌套——测试 3 处失败暴露，改直接查分支数组指令类型），全量验证通过（单元 20/20 + 冒烟 46/46 双协议 + 回归全绿 + 审计 PASS + DOM 级视觉确认）。

## 2. Oracle 审核要点闭环（方案级 → 实施级逐项）

| Oracle 项 | v0.2 方案 | 实施落点（main.js） | 验证 |
|---|---|---|---|
| **B1** 双状态 | 保留 editBlockLoopTarget（v1.14 零改动）+ 新增 editBlockIfTarget；互斥保证 + viewBlockEdit 重置 + 去抖各清一次 | editBlockIfTarget 声明/重置 + renderBlockEdit if 分支（loop 分支零改动）+ if chip 点击互斥清 loopTarget + renderBlockBranchInBlock | 冒烟 ✓（loop 编辑态回归 42/42 + if 双栏正常） |
| **B2** execIf 展开方式 | 共享引用逐帧确认安全（unshift/splice.apply 展开插入不改原数组）+ 缺口 #1 实证 | execIf 零改动（v1.5 splice.apply 展开插入）；无参数化含 if 块共享引用 | 冒烟 ✓（双调用后块库 then/else 未变 + _loopLeft 无驻留） |
| **B3** clone 统一入口 | cloneBlockBody(paramCmd) 顶层/loop.body/if 分支统一递归 + 参数注入；编辑器深拷贝 paramCmd=null | cloneBlockBody(paramCmd) + execCall 改 cloneBlockBody(def.body, cmd)；viewBlockEdit 深拷贝无参（缺省 undefined 同 null 语义） | 单元 ✓（执行副本注入/编辑副本保持 ?/独立副本）+ 冒烟 ✓（深拷贝取消未污染） |
| **B4** ifDepth 交互 | 宏展开累加说明 + hasIllegalNested 扩展 + 双重保险 | hasIllegalNested 扩展 if 分支 + hasCallInBlock 递归 if（v1.14 I4 已有） | 单元 ✓（if 分支嵌 if/loop/call 拒绝）+ 冒烟 ✓（多场景 0 JS 错误） |
| **B4 实施自发现** hasIllegalNested 递归漏检 | — | **真 bug 修复**：v0.1 递归实现查「子 if 内部」非「分支层」→ 改 hasForbiddenNested 直接查分支数组指令类型（与 loop.body 检查同模式） | 单元 3 处失败暴露 → 修复后 20/20 全过 |
| **I1** 双栏渲染 | 添加行复用 BLOCK_BODY_LOOP_CMDS（不新定义）+ 复用 .block-edit-row/.block-edit-col + 目标数组 if.then/else | renderBlockBranchInBlock（复用布局类 + BLOCK_BODY_LOOP_CMDS + renderBlockBodyChip 三态） | 冒烟 ✓（双栏独立添加 + 分支添加行 5 按钮无 if/loop） |
| **I2** 参数×条件注入 | 拷贝时解析（cloneBlockBody paramCmd 内 resolveStep，execIf 零改动） | cloneBlockBody(def.body, cmd) 统一注入 | 单元 ✓（then 槽0=2/else 槽1=4/存量 param:5 槽位感知）+ 冒烟 ✓（参数×条件执行） |
| **I3** 预览 .pv-if | 单层无递归 + 紫底 + {then…\|else…} + 分支内复用 .pv-steps/.pv-param | pickerPreviewSpan if 分支（buildBranch 子段） | 冒烟 ✓（.pv-if 类 + 摘要 {✅ ↑ \| ❌ ↰}）+ DOM 级视觉确认 |
| **I4** 教学引导 | 「重复出现的探测+决策模式才值得封装」 | 设计文档 §2.14 记录 | 文档 ✓ |
| **N1** 路线图 | 块内控制流嵌套未来开放 | 设计文档 §9 候选更新 | 文档 ✓ |
| **resolveStep null 守卫**（实施注意） | paramCmd=null 不访问属性 | cloneBlockBody 三态短路 `paramCmd ? resolveStep : c.steps` 天然满足 | 单元 ✓（paramCmd=null 保持 ? 占位） |

## 3. 变更摘要

| 文件 | 变更 |
|---|---|
| `main.js` | **+BLOCK_BODY_CMDS CMD_IF** + **editBlockIfTarget 双状态**（B1，保留 editBlockLoopTarget 零改动）+ **renderBlockEdit if 分支**（双栏 then/else + 返回/删除）+ **renderBlockBranchInBlock**（I1：复用布局类 + BLOCK_BODY_LOOP_CMDS + 三态交互）+ **renderBlockBodyChip if chip**（.if 紫底 + 点击进编辑 + 互斥清 loopTarget）+ **addBlockBodyCmd if 分支** + **cloneBlockBody(paramCmd) 统一入口**（B3：if.then/else 递归 + 参数注入 + steps 兜底保留）+ **execCall 改 cloneBlockBody(def.body, cmd)** + **hasIllegalNested 扩展 if 分支（hasForbiddenNested——真 bug 修复）** + **pickerPreviewSpan .pv-if 摘要**（I3）+ M 导出 |
| `style.css` | +`.pv-if` 紫底样式 |
| `tools.py` | version 1.14 → **1.15** |
| 文档 | 设计文档 §2.14（新增）/§7（v1.15 行）/§9 + 使用文档 §3.7（块里放分支 12-15）+ 矩阵 + 看板 + README + 系列 README + V1.15-{开发方案,审核报告} |

## 4. 验证证据（全量自动化，双协议 file:// + http://，--no-proxy-server）

| 项 | 结果 |
|---|---|
| 单元 | ✅ **20/20 PASS**（blockParamCount 递归 if 4 项 + cloneBlockBody paramCmd 统一入口 7 项含 B2 槽位感知/steps 兜底 + 星级 3 项含 if 递归 + hasIllegalNested/hasCallInBlock 6 项）；**3 处失败暴露 hasIllegalNested 漏检真 bug → 修复后全过** |
| 冒烟 | ✅ **46/46 PASS + 0 JS 错误**（双协议 ×23 项：+❓if chip .if 紫底/块内分支编辑态双栏/then-else 独立添加/分支添加行仅基础指令/返回块体/.pv-if 预览 + {✅↑\|❌↰} 摘要/调用/含 if 块执行（execIf 求值）/参数×条件执行/**execIf 展开不改原数组（共享引用双调用无污染实证）**/B4 多场景 0 JS 错误/深拷贝取消未污染/I2 保存拒嵌/I4 异常加载不崩/存量固定+单参预览/存储键/0 JS 错误） |
| 回归 | ✅ smoke_v114 **42/42** + smoke_v113 **54/54** + smoke_v112 **28/28** + smoke_v111 **54/54** + smoke_v110 **60/60**（1 处按钮数断言随 7 更新）+ smoke_v19 **38/38** + smoke_v18 **30/30**（全无破坏） |
| 视觉 | ✅ **DOM 级确认全正确**（looker 初报 2 处「偏差」经核实均为视觉误读）：then chips `["1.↑"]` / else `["1.↰"]`（OCR 误读箭头）+ 条目名 innerHTML `🧩 避障`（拼图 emoji 误读，同 v1.12 🔢→📐 先例）+ 预览 `❓if{✅ ↑ \| ❌ ↰}` 完全匹配；「(3) 后缀缺失」正确（无参数块不显示计数） |
| 构建 + 审计 | ✅ build_all v1.15 双模式成功；audit PASS（15 files 0 warning） |
| Chrome 61 | ✅ ES2018+ 扫描 0 命中（`??` 仅注释）；无新 API 越界（复用 makeEl/appendChild/splice.apply 等 ES5） |

## 5. 版本建议

| 项 | 建议 |
|---|---|
| 版本 | **v1.15**（minor：块体含 if——条件 × 抽象组合，条件抽象渐进） |
| Tag | `编程教学-v1.15`（发布前征求用户确认） |
| 遗留 | 块内控制流嵌套（if 内嵌 loop / 条件×循环，未来——设计文档 §9 候选）；真机 WebView 动态实测（全工具统一遗留） |