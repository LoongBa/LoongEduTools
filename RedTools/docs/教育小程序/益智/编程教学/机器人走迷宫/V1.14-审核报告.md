# 机器人走迷宫 V1.14 — 审核报告

> 版本：v1.14（块体含 loop：循环 × 抽象组合 + N2 预览视觉标记收尾）
> 审核范围：方案评审 + 实施结果复核
> 前置：`V1.14-开发方案.md`（Oracle v0.1 审：3B/4I/2N → v0.2 修订 → 复核通过）
> 审核日期：2026-09-25

---

## 1. 审核结论

**✅ 通过（Oracle 方案审核 ×2 轮 + 实施中自发现 resolveStep 固定值缺陷 + 全量自动化验证）**

Oracle 对 v0.1 审核：**B1（`_loopLeft` 污染推理错误 + needsCopy 过宽）+ B2（注入时机）+ B3（深拷贝递归）** + 4 I + 2 N + 7 验证缺口；
v0.2 修订后 Oracle 复核通过（逐帧验证 rep-on-cmd 顺序队列无污染成立、派生递归必要性、paramIdx 映射正确，无新增阻断项）。
实施严格按 v0.2 落地，**实施中自发现并修正 1 个真 bug**（resolveStep 丢失固定 steps 原样判断——测试 3 处失败暴露），
全量验证通过（单元 28/28 + 冒烟 42/42 双协议 + 回归全绿 + 审计 PASS + DOM 级视觉确认）。

## 2. Oracle 审核要点闭环（方案级 → 实施级逐项）

| Oracle 项 | v0.2 方案 | 实施落点（main.js） | 验证 |
|---|---|---|---|
| **B1** 副本推理修正 | 撤销 `_loopLeft` 污染推理（顺序队列无污染）；needsCopy = blockHasParam（递归版）；**派生递归化**（scanParamSteps 扫顶层 + loop.body + if） | blockParamCount/hasParam 改 scanParamSteps 递归；execCall needsCopy=hasParam；放弃 hasLoop 执行判定 | 单元 ✓（loop.body 仅参数 → paramCount=1，防共享引用崩溃）+ 冒烟 ✓（toggle 开 loop.body 变 ?₁） |
| **B1 前提验证** 无共享污染实证 | 验证缺口 #1/#2 | 冒烟 42/42：双调用无污染（块库 loop._loopLeft 未驻留存储）+ 参数字面确认 | ✓ 冒烟「块库 loop 对象无执行期字段」 |
| **B2** 注入时机 | 拷贝时同步解析；resolveStep 提取独立函数；execLoop 纯粹 | resolveStep 独立（B2 槽位回退链，含固定值原样保护）；execCall loop.body 递归注入 | 单元 ✓（参数×循环 params[2,4]→槽0=2 槽1=4；存量 param:5 槽1=3）+ 冒烟 ✓（参数×循环执行） |
| **B2 实施自发现** resolveStep 固定值 | — | **真 bug 修正**：resolveStep 初版无条件解析（固定 steps:2 被错误变 params[0]）→ 改 `steps !== null` 原样返回 | 单元 3 处失败暴露 → 修正后 28/28 全过 |
| **B3** 深拷贝递归 | cloneBlockBody 统一递归逐字段（id/rep/steps/paramIdx + loop.body） | cloneBlockBody 递归 loop.body；viewBlockEdit 改用 | 单元 ✓（loop.body paramIdx 复制 + 独立副本）+ 冒烟 ✓（编辑取消块库未污染） |
| **I1** 去抖隔离 | 进 loop 编辑态 suppressChipClickUntil=0；paramIdx 指向顶层 | renderBlockEdit loop 分支入口重置；renderBlockBodyChip arr 参数化 | 冒烟 ✓（loop 编辑态交互正常） |
| **I2** 单层约束 | 三层：入口隔离 + 保存校验 + 执行防御 | BLOCK_BODY_LOOP_CMDS（不含 loop/if/call）+ doSaveBlock hasIllegalNested + hasCallInBlock | 单元 ✓（嵌 loop/if/call 拒）+ 冒烟 ✓（保存被拒提示） |
| **I3** 激励示例表 | §2.4 三组对比表 + 阈值宽松可接受 | 文档 §2.13 记录 | 单元 ✓（loop rep:6 计 2 vs 直接 7） |
| **I4** 防嵌套递归 | hasCallInBlock 递归 loop.body/if.body/if.alt | hasCallInBlock 递归全结构 | 单元 ✓（loop.body/if 分支含 call true） |
| **N1/N2** | N1 扩展路径记录；N2 预览 span 拼接 | 文档 §9 候选更新（块体含 if 未来）；pickerPreviewSpan + .pv-* CSS | DOM ✓（pv-loop/pv-param/pv-steps 类 + 循环内嵌套 ?₁ 带类） |
| **7 验证缺口** | 全部列入冒烟/单元 | §4.2 逐一对应 | 详见第 4 节 |

## 3. 变更摘要

| 文件 | 变更 |
|---|---|
| `main.js` | **+scanParamSteps/blockParamCount 递归化**（B1）+ **blockHasLoop**（文档用）+ **cloneBlockBody 递归深拷贝**（B3）+ **BLOCK_BODY_CMDS + CMD_LOOP** + **BLOCK_BODY_LOOP_CMDS**（I2 入口隔离）+ **renderBlockEdit loop 编辑态分支**（editBlockLoopTarget + 次数调参 + loop.body chips 复用 + 返回/删除 + I1 去抖重置）+ **renderBlockBodyChip arr 参数化 + loop chip 渲染/点击进编辑** + **hasEditBodyParam/hasParamInBody 递归派生** + **toggleBlockHasParam 递归**（toggleStepsInArr/hasStepsInArr）+ **addBlockBodyCmd 目标数组 + loop 分支** + **doSaveBlock hasIllegalNested 校验**（I2）+ **execCall needsCopy=hasParam + loop.body 递归注入 + resolveStep 提取（含固定值修正）+ hasCallInBlock 递归**（I4）+ **pickerPreviewSpan 预览 span 拼接**（N2）+ M 导出 + hint 文案 |
| `style.css` | +`.pv-steps/.pv-param/.pv-loop/.pv-inner`（预览视觉标记） |
| `tools.py` | version 1.13 → **1.14** |
| 文档 | 设计文档 §2.13（新增）/§7（v1.14 行）/§9 + 使用文档 §3.7（块里放循环 8-11）+ 矩阵 + 看板 + README + 系列 README + V1.14-{开发方案,审核报告} |

## 4. 验证证据（全量自动化，双协议 file:// + http://，--no-proxy-server）

| 项 | 结果 |
|---|---|
| 单元 | ✅ **28/28 PASS**（B1 派生递归 8 项含 loop.body 仅参数/I4 clamp + B3 深拷贝 4 项 + 星级 5 项含 I3 激励 + execCopy 参数×循环 4 项含 B2 槽位感知/固定值 + 单层校验/防嵌套 7 项）；**3 处失败暴露 resolveStep 固定值缺陷 → 修正后全过** |
| 冒烟 | ✅ **42/42 PASS + 0 JS 错误**（双协议 ×21 项：+🔁循环 chip/块内循环编辑态/次数调参/loop.body 内部指令/toggle 递归开 loop.body ?₁/面板 loop 摘要 + pv-loop + pv-param/调用 (3)/含 loop 参数块执行/**块库 loop 无执行期字段（无污染实证）**/参数×循环执行/**深拷贝取消块库未污染**/I2 保存拒嵌 loop/I4 异常加载不崩/存量固定+单参预览/存储键/0 JS 错误） |
| 回归 | ✅ smoke_v113 **54/54** + smoke_v112 **28/28** + smoke_v111 **54/54** + smoke_v110 **60/60**（1 处按钮数断言随 6 更新）+ smoke_v19 **38/38** + smoke_v18 **30/30**（全无破坏） |
| 视觉 | ✅ **DOM 级确认全正确**（looker 初报 2 FAIL 经核实为截图脚本缺陷——toggle 前无 steps 致参数化未激活）：DOM 实态 `loop.body chips: 1.➡?₁步` + `.param-step` 浅紫类 ✓ / loop chip `.loopb` 绿底（v1.7 主序列循环块配色）✓ / 条目 `🧩 圈x (3)` ✓ / 预览 `pv-loop:🔁×2{➡?₁步}` + 嵌套 `.pv-param` ✓（N2 低龄分辨核心达成）；补 `.pv-inner` CSS |
| 构建 + 审计 | ✅ build_all v1.14 双模式成功（0.05 MiB）；audit PASS（15 files 0 warning） |
| Chrome 61 | ✅ ES2018+ 扫描 0 命中（`??` 仅注释）；slice(0,3)/push 均 ES5；pv-* class 全引用一致 |

## 5. 版本建议

| 项 | 建议 |
|---|---|
| 版本 | **v1.14**（minor：块体含 loop——循环 × 抽象组合 + N2 预览视觉标记收尾） |
| Tag | `编程教学-v1.14`（发布前征求用户确认） |
| 遗留 | 块体含 if（条件抽象渐进，未来——设计文档 §9 候选）；主序列贪心优化/真机 WebView 动态实测（全工具统一遗留） |