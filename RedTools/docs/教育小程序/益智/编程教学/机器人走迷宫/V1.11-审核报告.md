# 机器人走迷宫 V1.11 — 审核报告

> 版本：v1.11（块参数 = 函数参数化抽象进阶 + N-3 遗留修复）
> 审核范围：实施结果复核
> 前置：`V1.11-开发方案.md`（v0.2 修订版，Oracle 复核通过）
> 审核日期：2026-09-25

---

## 1. 审核结论

**✅ 通过（自我复核）**

方案 v0.2 经 Oracle 两轮评审（v0.1 评审 2B/3I/3N → v0.2 修订复核通过）。
实施后 Oracle 代码级复核**两次尝试均因 token 配额耗尽中断**（资源限制，非代码问题）——
本报告基于**自我复核**（对照 v0.2 方案 8 点逐项核对源码）+ 全量自动化验证证据。

## 2. 自我复核（对照 v0.2 方案逐项核对源码）

| 方案点 | 实现位置 | 核对结论 |
|---|---|---|
| B2 hasParam 派生（不存储，body.some steps===null） | blockHasParam L97-108（带 id 守卫）；viewBlockEdit L487 `editBlockHasParam = blockHasParam(id)`；删除参数化 steps → 重新派生 L635 | ✅ 单一数据源，谓词一致（带 id 守卫），删参自动 off |
| B1 N-3 来源记忆全入口 | blockListReturn 声明 'home' L429；viewHome() 开头重置 L995（覆盖页头 home/任意逃逸）；首页卡片设 'home' L1025；mgrBtn 设 'game' L1389；viewBlockList 按来源「返回游戏/返回首页」L469-477 | ✅ 四入口全设，无 stale 可能 |
| I1 totalSteps 统一 | L1977 `n += totalSteps(bd.body)`（参数/非参数统一；steps:null 经 c.rep\|\|1 计 1） | ✅ 无手动计分支（Oracle 确认等价） |
| I2 运行时副本 | execCall L1770-1781：blockHasParam → map 浅拷贝（id/rep/steps 逐字段）+ steps:null→param（`cmd.param||3`）；无参数块共享引用；resetLoopLeft 保留（注释空操作说明 L1768） | ✅ 副本隔离正确，块库永不被写参数值 |
| I3 4 处 guard | L618（块编辑 body）/L1315（主序列 cycleSteps）/L1517（loop body）/L1600（if 分支）`if (steps===null) return;` | ✅ 4 处全覆盖 |
| 参数版显示 | renderBlockBodyChip `➡N步(N)` + .param-step；renderBlockPicker `🧩名(3)`+预览 `➡N步`；renderSeq `🧩名(N)` + .param-call | ✅ |
| cycleCallParam | L1320-1325 `((param||3)%9)+1`（3→4→…→9→1→2→3，对齐 steps 循环） | ✅ 与既有交互一致 |
| 执行兼容 | 展开后为普通 steps:N → execStep 逐格动画/撞墙停止零改动 | ✅ |
| 附修复（renderSeq 补渲染） | viewBlockList 返回游戏 L474 + exitBlockEdit L681 补 renderSeq（renderGame 不重建序列 chips） | ✅ v1.10 遗留修复 |

## 3. 验证证据（全量自动化，双协议 file:// + http://）

| 项 | 结果 |
|---|---|
| Node totalSteps 单元 | ✅ 14/14（参数块单/双/三调用 body 计一次 + param 值不影响计数 + 非参数零回归 + 激励 块5 vs 展开6 + 既有口径 + 孤儿） |
| Playwright 冒烟 | ✅ 54/54 + 0 JS 错误（参数 toggle/无 steps 提示保持 off/参数版 no-op/保存参数块/面板 `🧩名(3)`/cycleCallParam 3→4/无参数块点击分流/N-3 返回游戏序列保留+B1 无 stale/B2 删参自动 off/旧块零迁移/存储键/0 JS 错误） |
| Playwright 执行专项 | ✅ 12/12 + 0 JS 错误（参数块 param=3 推箱通关 + 双参数(3)+(2) 执行通关 + **执行后块库 steps 仍 null（副本隔离核心）**） |
| 回归 | ✅ smoke_v110 60/60 + smoke_v19 38/38 + smoke_v18 30/30（既有指令/块/编辑器/循环嵌套无破坏） |
| 视觉质检 | ✅ 5/5（01 编辑器参数 toggle active+参数版 chip / 02 面板 🧩名(3)+预览 / 03 序列 🧩名(3) / 04 调参后 (4) / 05 N-3 返回游戏按钮） |
| 构建 + 审计 | ✅ build_all offline/online 双模式；audit PASS（15 files 0 warning） |
| Chrome 61 | ✅ 全 var/function、无 ?./??/Set/Map/对象展开；CSS 全 -webkit- flex 前缀 |

## 4. 变更摘要（对照 v1.10）

| 文件 | 变更 |
|---|---|
| `main.js` | +blockHasParam 派生 / +块编辑器参数 toggle（editBlockHasParam 从 body 派生 + toggleBlockHasParam 返回错误消息）/ +renderBlockBodyChip 参数版 `➡N步(N)` + ✕ 删除重新派生 + 点击 no-op / +renderBlockPicker 参数显示 + mgrBtn 来源 / +renderSeq call chip 参数分流（cycleCallParam vs 进编辑）/ +cycleCallParam / +execCall 运行时副本（map 浅拷贝 + steps:null→param）/ +N-3 blockListReturn 全入口贯通（viewHome 兜底 / 首页卡片 / mgrBtn / 返回游戏）+ renderSeq 补渲染修复（exitBlockEdit + viewBlockList）/ +4 处 I3 guard / M 导出扩展 |
| `style.css` | +参数样式（.block-param-toggle.active / .param-step / .param-call / .block-edit-param-hint） |
| `tools.py` | version 1.10 → 1.11 |
| 文档 | 设计文档 §2.10/§3/§4/§6/§7/§9 + 使用文档 §3.7/FAQ + 矩阵 + 看板 + 系列 README + RedTools README |

## 5. 版本建议

| 项 | 建议 |
|---|---|
| 版本 | **v1.11**（minor：新功能块参数——函数参数化抽象进阶 + N-3 修复） |
| Tag | `编程教学-v1.11`（发布前征求用户确认） |
| 遗留 | N1（多处参数化）/N2（定义调用显示分离）记后续增强；Chrome 61/真机未实测（全工具统一遗留） |
| 备注 | 实施后 Oracle 复核因 token 配额耗尽中断 2 次（资源限制）——本报告为自我复核（对照 v0.2 方案逐项核对 + 全量自动化验证）；方案级 Oracle 审核（v0.1 评审 + v0.2 复核通过）已完成 |