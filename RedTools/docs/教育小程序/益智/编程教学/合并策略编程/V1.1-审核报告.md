# 合并策略编程 V1.1 — 审核报告

> 审核范围：V1.1 关卡编辑器（创作闭环）——方案审核（两轮）+ 实施后代码复核 + 验证证据
> 更新：2026-09-26

---

## 1. 审核范围

| 项 | 说明 |
|---|---|
| 版本 | v1.1（minor：新功能关卡编辑器——创作闭环） |
| 前置 | v1.0（确定性合并 + 方向指令序列 + BFS 最短解星级，已发布） |
| 方案 | `docs/教育小程序/益智/编程教学/合并策略编程/V1.1-开发方案.md`（v0.2 终版） |
| 变更文件 | `src/assets/main.js`（687→1067 行）、`src/assets/style.css`、设计/使用文档、矩阵/看板/README/tools.py |

## 2. 方案审核记录

| 轮次 | 审核方 | 结论 | 关键意见与处理 |
|---|---|---|---|
| v0.1 | Oracle（架构） | **通过（0 B + 4 I + 3 N）** | I1：maxDepth=12 对 target=32 极端盘面假阴性 → maxDepth 18 +「18 步内未找到解」文案；I2：nodeCap 超限返回值未明确 → 返 -2 三态；I3：startCustom 缺 finished → 补；I4：viewCustomList 缺 stopTimer → 补。N2：state.size 统一减守卫点 |
| v0.2 | Oracle（架构） | **复核通过（0 B + 4 I + 3 N）** | I-1：§1.3「零改动」矛盾 → 改「仅增可选 nodeCap 参数」；I-2：§7 风险表 9 处残留 → 改 7 处 + viewCustomList；I-3：「缩小盘面」文案不可行（4×4 固定）→ 改「减少块数」；I-4：nodeCap -2 测试非确定 → 补低 nodeCap 确定性测试。N-1/N-2/N-3 全部入文 |

## 3. 实施后代码复核（Oracle）

**结论：复核通过（0 B / 0 I / 0 N）**——12 项自查全 ✓：

1. bfsShortest 第 5 形参 nodeCap（默认不启用）→ queue.length > nodeCap 返 -2；存量 genLevel 未传 ✓
2. state.size 统一：startGame=LEVELS_CFG[level].size、startCustom=4；renderBoardOnly/execStep 读 state.size，无残留 ✓
3. state.finished 五处重置（viewHome/startGame/startCustom/viewCustomList/viewEditor）✓
4. checkCustom 三态文案与方案逐字一致（-1「18 步内未找到解」/ -2「暂不确定，试试减少块数」）✓
5. customLevels 数据层走 storage API，无硬编码完整键名；上限 20 + id 唯一 ✓
6. startCustom 完整重置（含 stopTimer 防重载二次进入）✓
7. finishLevel custom 分支：不写 best/recent、真实星级、markCustomSolved、history 含 customName ✓
8. custom 守卫边界：nextLevel/重置/viewParent history/清除数据/stopTimer 全到位 ✓
9. M 导出 8 函数 ✓
10. 编辑器交互：点格循环 + 工具直写 + pointer capture + 单格 class 更新（防 capture 丢失）✓
11. 名称输入不改 editChecked（实施中修复的过度防御）✓
12. ES2017/Chrome 61/无外部资源/存储键 redtools.hebing 前缀 ✓

## 4. 验证证据

| 项 | 结果 |
|---|---|
| 单元测试（Node，与前端同源） | **16/16**（空盘面/上限/值域/sum 硬约束/单块不可解/简单可解/不可达样例/普通/挑战可达/BFS 口径一致/nodeCap -2 确定性/常规不误触/生成器互逆 100 关 optimal 一致/optimal 回放/三态区分） |
| Playwright 冒烟（file:// + http:// 双协议） | **48/48**（首页入口/新建 4×4/点格循环/工具涂格/原子量拦截/保存拦截/校验可解/保存→列表/挑战通关 3★/下一关边界/已解徽章/重置边界/编辑已有/存储键回归/清除数据覆盖/存量回归/0 JS 错误） |
| 构建 + 审计 | offline+online 成功；audit PASS（6 files 0 warning） |
| Chrome 61 静态复核 | PASS（无 ?./??/模板字符串/箭头函数/flex gap；仅注释命中） |
| 实施修复 | 名称输入监听不再重置 editChecked（改名不影响可解性——冒烟发现的过度防御） |

## 5. 变更摘要

| 文件 | 变更 |
|---|---|
| main.js | bfsShortest 增 nodeCap 参数（-2 三态）；state.size 统一；customLevels 数据层（load/save/add/remove/markCustomSolved，20 关上限）；viewHome 自建关卡入口 + viewCustomList + viewEditor（点格循环/工具/校验/保存）；checkCustom 三态校验（maxDepth 18 + nodeCap 20 万）；startCustom；7 处 custom 守卫 + viewCustomList stopTimer；finishLevel custom 分支（真实星级/markCustomSolved/history customName）；nextLevel/重置 custom 边界；viewParent history custom 显示 + 清除数据覆盖；M 导出 8 函数 |
| style.css | 编辑网格（2/4 配色）/工具条/关卡库列表/自建关卡卡片/btn-sm |
| 设计文档 | §2.5 自建关卡 + §6 v1.1 行 + §8 路线更新 |
| 使用文档 | §自建关卡操作 + FAQ |
| 矩阵/看板/README/tools.py | v1.1 同步 |

## 6. 版本建议

| 项 | 建议 |
|---|---|
| 版本 | **v1.1**（minor：新功能关卡编辑器）——待用户确认 |
| Tag | `合并策略-v1.1`（AGENTS.md §6 P0 工具专属 tag 约定）——待用户确认 |
| 遗留 | 无阻断项；⚠️ Chrome 61/真机未实测（如实标注） |

## 7. 合规自检

- JS：ES2017 经典脚本、无内联事件、无 eval；CSS：无 flex gap/aspect-ratio/clamp/:has
- CSP：无 fetch/XHR、无外部 http(s) 引用；数据最小化（纯离线 localStorage）
- 自建关卡本地存储、不入 publish（可解校验 = UX 保障非合规硬要求）；无 PK/排行榜/云存档
