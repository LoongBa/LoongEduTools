# V0.5 跨工具离线能力 — 审核报告（guard 统一接入）

> 审核范围：V0.5 防沉迷/自控力 guard 模块补齐 + 数学口算样板接入（跨工具离线能力第一迭代）
> 更新：2026-09-26

---

## 1. 审核范围

| 项 | 说明 |
|---|---|
| 版本 | V0.5（开发支撑子系统迭代，非具体工具；含数学口算 v1.1 → v1.2 minor） |
| 前置 | V0.4（双模式 B 批逻辑迁移）；V0.5 存量审计报告 |
| 方案 | `docs/教育工具/V0.5-跨工具离线能力-开发方案.md`（Oracle v0.1 审 0B/3I/多N → v0.2 定稿） |
| 变更文件 | `_shared/js/src/guard.js`（重写）、`series/学科/数学口算/src/assets/main.js` + `style.css`、`docs/通用需求-防沉迷自控力.md`、tools.py（数学口算 version 1.2） |

## 2. 方案审核记录

| 轮次 | 审核方 | 结论 | 关键意见与处理 |
|---|---|---|---|
| v0.1 | Oracle（架构） | **0 B + 3 I + 多 N** | **I-1 计数单位不匹配**（§3 契约「按完成题数计」vs 方案每组 +1 → 10 倍偏差）→ addPlayed 增 count 参数。**I-2 curStartedAt 位置**（§4 在 cur）→ 改调用方传参（guard 不持久化局状态）。**I-3 cfg.keySelf 映射不清** → 废弃 keySelf，固定顶层字段名。N-1~N-8 全部处理（isLocked 以 selfDaily 为准 / version 升 2 / extend 术语 / setPrefs 不重置 / streak 格式 / enough 签名 / 学科豁免休眠 / 旧 API deprecated） |
| v0.2 | Oracle（架构） | **复核通过（0 B / 0 I / 0 N）** | 方案定稿 |

## 3. 实施后代码复核（Oracle）

**结论：复核通过（0 B / 0 I / 4 N）**——5 大点全 PASS（guard.js 正确性：schema/backfill/setPrefs 非钳制/addPlayed 计数+豁免边界/checkDue 数学/extend 上限/enough+resetIfNewDay/isLocked selfDaily 设计；数学口算接入：normalizeStore v1→v2 保字段/startQuiz 锁位置/finishQuiz 计数+到点/结算+浮层；UX：alert 兼容/scope 纯净/47 款 0 消费零回归/IIFE 无副作用/es2017）。

4 项 N 处理：
| N | 内容 | 处理 |
|---|---|---|
| N-1 | showLimitOverlay 忽略 extend 返回值（达上限点延迟静默关闭） | ✅ 已修：extend 返 false → 浮层保持不关，仅剩自律出口 |
| N-2 | selfLocked 死状态（isLocked 用 selfDaily 不读它） | ✅ 文档注：保留兼容契约 §4 schema，不参与判定 |
| N-3 | checkDue elapsedMs 为单局非当日累计（分钟档学科类惰性） | ✅ 文档注：学科类题量口径，分钟档惰性属预期（通用需求 doc 已补注） |
| N-4 | **ISSUE-4 竞态**：400ms 内点结算「我很自律」→ 待调度浮层仍弹出 | ✅ 已修：setTimeout 回调复查 isLocked（冒烟新增专项断言） |

## 4. 验证证据

| 项 | 结果 |
|---|---|
| guard 单元测试 | **31/31**（setPrefs 钳制+保档/不重置计数/startRound 跨日重置/addPlayed count 计数/试错豁免 <10s 不计/count=10+5 累计/checkDue min+games 双触发/extend 双类型+上限 2+非法类型/enough 自律锁+streak/isLocked 跨日复位/旧 API deprecated 保留/空 store 兜底） |
| Playwright 冒烟（file:// + http:// 双协议） | **42/42**——防沉迷全链路：首页设置行渲染+档位切换持久化（version=2 + guard 字段）/进练习/完成一组结算/guard 计数 playedToday=10/结算「我很自律」动作写入（selfDaily+selfLocked+streak=1）/自律锁拦截（选模式后不进键盘）/到点浮层出现/延迟 delayGamesTimes=1/**ISSUE-4 竞态专项（点结算自律后 400ms 内浮层不出现 + 回首页）**/存储键 redtools.math.v1 无 .v1. 双写/schema v2 完整/打卡日历·错题本·成就存量入口/0 JS 错误 |
| 构建 + 审计 | offline + online 成功；audit PASS（**7 files 0 warning**） |
| Chrome 61 静态 | PASS（无 ?./??/模板字符串/箭头函数/flex gap；仅注释命中） |
| 存量回归 | 47 款注入 lx-shared.js 无回归（guard 0 消费 + IIFE 无副作用 + 旧 API 保留）；fetch 抽查构建产物含新 API 注入完整 |

## 5. 变更摘要

| 文件 | 变更 |
|---|---|
| `_shared/js/src/guard.js` | **补齐至通用需求 §4 完整契约**：新增 8 API（setPrefs/startRound/addPlayed/checkDue/extend/enough/isLocked/streak）+ schema（store.guard 内嵌 {minutePref,gamesPref,today,playedToday,delayMinTimes,delayGamesTimes} + 顶层 selfDaily/selfStreak/selfLocked）；旧 4 API 保留 + @deprecated；ES2017/Chrome 61 |
| `series/学科/数学口算/src/assets/main.js` | **v1.2 样板接入**：normalizeStore v1→v2（补 guard/selfDaily 默认值保字段）；首页「练习限时」设置行（分钟 5/10/15 + 题量 10/20/30/不限）+ 提示；startQuiz 自律锁拦截 + startRound；finishQuiz addPlayed(count=total) + checkDue + 到点浮层（延迟/自律，ISSUE-1/4 已修）；结算「我很自律」动作 |
| `style.css` | limit-row/limit-btn/limit-overlay 样式（复用 24点 同款，Chrome 61：-webkit 前缀 + margin 分隔） |
| `docs/通用需求-防沉迷自控力.md` | 落地表补 V0.5（guard 补齐 + 口算样板 + 三款内联保留决策） |
| tools.py | 数学口算 version="1.2" |

## 6. 版本建议

| 项 | 建议 |
|---|---|
| 数学口算版本 | **v1.2**（minor：新功能防沉迷/自控力）——待用户确认 |
| Tag | `数学口算-v1.2`（AGENTS.md §6 工具专属 tag 约定；dist 构建版本已 1.2）——待用户确认 |
| 公共模块 | lx-shared.js 构建注入（无独立版本）；V0.5 迭代文档记录 |
| 遗留 | ⚠️ Chrome 61/真机未实测（跨工具全局遗留，如实标注）；分享卡片模块化/uikit feedback 抽取（后续迭代已排入 todo） |

## 7. 合规自检

- guard.js 纯状态判定库，无 DOM/网络/外部引用；数据最小化（仅档位/今日计数/延迟次数/自律天数）
- 数学口算：localStorage 键不变（redtools.math.v1）；新增字段纯本地；无 PK/排行/云存档
- 防沉迷 = 软提醒（延迟/自律二选）+ 自律成就，无强制锁死对抗（通用需求 §5 设计思想）
- ES2017 经典脚本 + Chrome 61 基线（审计 PASS）