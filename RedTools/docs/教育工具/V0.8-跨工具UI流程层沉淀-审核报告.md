# V0.8-跨工具UI流程层沉淀-审核报告

> 状态：审核报告（V0.8 交付）
> 日期：2026-09-26
> 方案：`V0.8-跨工具UI流程层沉淀-开发方案.md`（v0.1 Oracle NO-GO 2B/4I/5N → v0.2 修订）

## 1. 审核范围

| 项 | 内容 |
|---|---|
| 新增 | `_shared/js/src/ui.js`（LX_SHARED.ui，11 API UI 流程层模块） |
| 修改 | `storage.js`（+migrate 模板方法）、`progress.js`（+longestStreak 写回 + P.longest）、`lx-shared-core.js`（+ui 槽位 + __loaded.ui）、`build_framework.py`（merge order +ui.js） |
| 迁移 | `series/学科/数学口算/src/assets/main.js`（v1.2.1→v1.2.2，1312→1181 行，-134 净删内联） |
| 版本 | tools.py 数学口算 1.2.1 → 1.2.2（patch） |

## 2. 验证证据

| 验证 | 结果 |
|---|---|
| 单元测试 `unit_ui_v08.js` | **39/39**（含 Oracle B2 硬断言 `LX_SHARED.ui` 存在 + `__loaded.ui`） |
| v1.2 回归冒烟 `smoke_math_v12.js` | **44/44**（file:// 全量 + http 0 JS 错误；适配结算浮层断言） |
| V0.8 专项冒烟 `smoke_math_v122.js` | **12/12**（设置条/结算三选/拦截/日历/曲线/到点/分享 + B2 断言） |
| 回归 | guard 31/31 + uikit 11/11 + share 16/16（**58/58**） |
| 审计 | 7 files 0 warning PASS |
| CSS 零改动 | `git diff style.css` **0 行** |
| 产物注入 | dist lx-shared.js 含 ui 槽位/11 API/migrate/longestStreak；data.js version 1.2.2 |
| Chrome 61 静态复核 | 无 ?./??/箭头/模板字符串/对象展开（Oracle 核对确认） |

## 3. Oracle 复核结论（实施后）

**GO**（0 阻断 / 1 改进 / 3 备注）：

| 编号 | 等级 | 内容 | 处置 |
|---|---|---|---|
| I-1 | 改进 | settleChoices stars 摘要段硬编码 `lx-overlay-summary/lx-stars` 未入 cls 配置；数学口算 CSS 无此类 → ★★★ 居中可读但未美化 | **记 V0.8.1 微调待办**（加 cls.summary/cls.stars），不阻塞本轮 |
| N-1 | 备注 | 结算「再来一局」未按 §2.4 契约条件隐藏（功能等价：locked 点再来→lockHint 拦截） | 契约精神满足，功能闭环 |
| N-2 | 备注 | storage.migrate 版本字段名硬编码（全库统一 version，无实务影响） | 措辞偏差，无需改 |
| N-3 | 备注 | 400ms 内点「再来一局」→ stale pauseFlow 跨局触发（v1.2 既有边界，非本轮引入） | 超 V0.8 范围，不阻塞 |

## 4. 变更摘要（方案 v0.2 修订落地）

| 方案项 | 落地 |
|---|---|
| A1 checkinCalendar | ✅ 打卡日历组件（日历/cal-day/done/today/streak-info） |
| A2 lineChart | ✅ SVG 进步曲线（line-chart，Chrome 61 createElementNS） |
| A3 saveImage | ✅ 保存图片双路径（桌面 `<a download>`+兜底 / 移动长按提示） |
| A4 settleChoices | ✅ 结算三选（复用 uikit.overlay 缺省 / cls 可配走 overlayLocal） |
| A5 guardBar | ✅ 设置条（limit-btn/active） |
| A6 selfBadge | ✅ 自律徽标 canvas 绘制 |
| A7 selfStreakCard | ✅ 自律成就卡（progress.longest 读取） |
| A8 migrate | ✅ 移入 storage.js（Oracle B1） |
| A9 shareText | ✅ 分享文案纯函数（template 参数逐字节保形） |
| A10 getShareCapability | ✅ 能力探测（navigator.share && https 双守卫） |
| A11 lockHint | ✅ 自律锁拦截提示（替代 window.alert） |
| A12 pauseFlow | ✅ 到点暂停浮层（{el,close}+回调，onExtend false 保持） |
| B2 槽位硬验收 | ✅ lx-shared-core ui 槽位 + __loaded.ui + 单测硬断言 |
| 口径 #17/#18/#20 | ✅ 保留代码现状（redtools.<tool>.<key> / history≤30 / startedAt 调用方传），文档对齐 |
| 口径 #19 longestStreak | ✅ progress 写回 + P.longest API |

## 5. 版本与发布建议

- 数学口算 **v1.2.2**（patch：行为不变内部替换 + 补齐防沉迷 §2.4 结算三选契约）
- tag `数学口算-v1.2.2`（工具专属前缀）
- 待用户确认版本后 push + tag

## 6. 遗留与后续

1. **I-1**：settleChoices stars 摘要类名配置化（V0.8.1 微调，数学口算传 cls.summary/cls.stars）
2. **N-3**：400ms stale pauseFlow 竞态（可加 state.settleToken 令牌），超范围不排期
3. **8 款内联分享迁移**（24点/成语接龙/看图猜成语/扫雷推理/数独思维等 + 🔒 点击反应/手速/经典扫雷）：后续按需迁移至 LX_SHARED.ui（方案 §7-N5，单列轮次）
4. **其余工具 UI 流程迁移**：按需接入 ui.js 11 API（设置条/日历/曲线/结算三选等）
5. ⚠️ Chrome 61/真机未实测（全局遗留，如实标注）
