# 机器人走迷宫 — 设计文档

> 益智系列 · 编程教学子系列首款工具（D1 原型至 v1.3）。
> 产品定位见 `docs/教育工具/教育工具产品矩阵规划.md` §10 阶段 D（推迟款转型-编程教学方向）
> 与 `docs/教育工具/编程教学产品线-立项评估.md`；系列规范见 `series/益智/编程教学/README.md`。
> 更新：2026-09-22

---

## 1. 产品概况

| 项 | 说明 |
|---|---|
| 工具名 | 机器人走迷宫 |
| 系列 | 益智 · 编程教学（子系列，D1 立项） |
| 版本 | v1.3 |
| 优先级 | P2（D1 编程教学原型 → 产品化） |
| 目标用户 | 小学生 / 编程启蒙（指令序列 → 循环 → 条件） |
| 核心价值 | 把「指令序列编程」做成有标准答案的练习：编排机器人指令 → 盘面判定（合规安全区） |
| 形态 | 离线 zip 静态工具；SVG 程序化盘面零素材；复用推箱子关卡/解析/推箱逻辑 |

**合规定位（关键）**：无计分对抗/等级排行/云存档/多人互动/内购/广告，超出玩法为「指令序列 +
目标盘面判定」，**有标准答案**（编程练习，非表现性分数）→ 指导原则 §二安全区。立项依据见
`docs/教育工具/编程教学产品线-立项评估.md`。

## 2. 玩法设计

### 2.1 规则

- 迷宫盘面 = 推箱子关卡（Microban I，XSB 格式）：墙、地板、目标点、箱子。
- 孩子用**指令积木**编排机器人的移动序列：`↑ 前进` / `↰ 左转` / `↱ 右转` / `🧱 前方探测`。
- 点「▶ 执行」后机器人按指令序列移动；**前方有箱子则推动**（推箱逻辑同推箱子）。
- 胜利条件：执行完成后**所有箱子都在目标点**（isWon 判定）= 有标准答案。
- 失败不惩罚：执行完箱子未到位 → 提示「还有箱子没到目标点，调整指令再试」。

### 2.2 指令系统（v1.0 → v1.3，编程教学核心概念路线）

| 版本 | 概念 | 指令 | 说明 |
|---|---|---|---|
| v1.0 | 顺序 | ↑ 前进 / ↰ 左转 / ↱ 右转 | 顺序编排 + 观察调试 |
| v1.1 | 循环 | 指令 chip ×2/×3/×4 | 单击 chip 循环次数递增（1→2→3→4→1），循环启蒙 |
| v1.3 | 条件 | 🧱 前方探测 | **探测前方**：前方为墙/箱子/边界 → 条件成立「不前进」；前方空地 → 前进一格（避障守卫） |

- **🧱 前方探测（v1.3 新增）**：一个「安全前进/避障」条件指令——机器人先看前方有没有障碍，
  没有才动。孩子用它避免「撞墙/撞箱」，为后续 if/else 条件分支（前方有墙则……）打基础
  （低龄认知考量：完整 if/else 留待后续，见 §9 概念路线）。
- 单条指令可带循环次数（rep），循环在**执行时展开**：指令实际执行 rep 次，动画可见。
- 星级基准 = **实际执行步数**（循环展开后 totalSteps）vs 关卡最优推动数 b。

### 2.3 交互

| 输入 | 说明 |
|---|---|
| 点指令按钮 | 追加指令到序列（右侧序列展示 1.↑ 2.🚧…） |
| 点序列 chip | 循环次数递增（1→2→3→4→1），chip 显示 ×N |
| 点 chip ✕ | 删除该条指令 |
| ↺ 清空 | 清空指令序列 |
| ▶ 执行 | 从初始盘面执行全部指令（每步 260ms 动画） |
| ⟲ 重置 | 重置到初始盘面（保留序列） |
| 下一关 › | 通关后出现，进入下一关 |

## 3. UI 设计

```
┌──────────────────────────┐
│ v1.3                     │ ← header 版本小字
│ [‹ 返回] 简单 · 第 1 关 ⏱ 00.0 │ ← 顶栏：返回 + 关卡信息 + 计时
│ 目标：把箱子推到橙色圈     │
│ ┌──────────────────────┐ │
│ │ ▓▓▓▓▓▓              │ │ ← 迷宫盘面（SVG，墙灰/箱棕/目标橙圈/玩家蓝球）
│ │ ▓   ◎▓              │ │
│ │ ▓▓▓ ▓▓▓▓            │ │
│ │ ▓■⊙▓▓              │ │
│ │ ▓▓▓ ▓▓▓▓            │ │
│ └──────────────────────┘ │
│ [↑ 前进] [↰ 左转] [↱ 右转] [🧱 前方探测] │ ← 指令区
│ 1.↑ 2.🧱(×3)          [↺ 清空] │ ← 指令序列（chip 点击循环/✕ 删除）
│      [▶ 执行] [⟲ 重置]        │
│ 机器人执行中… / ✓ 完成！…    │ ← 反馈
│ 完成一关点亮今日·已连续N天   │ ← footer 打卡
└──────────────────────────┘
```

- 明快儿童友好配色（沿用益智系）：米黄底 / 墙 #8d8f98 / 箱 #b76a1a / 目标橙圈 / 玩家蓝球。
- 手机竖屏为主；PC 居中 max-width 480px。
- 难度页三卡（简单/普通/挑战）显示各自最佳成绩；选关后进入练习视图。

## 4. 技术设计

### 4.1 数据模型

- **关卡数据**：`window.LEVELS = [{g:[XSB行], b:最少推动数|null}]`（levels.js，更名自推箱子，
  Microban I 155 关全量内嵌，b 由求解器预计算）。
- **难度区段**（`LEVELS_CFG`）：easy 1-30 / normal 31-80 / hard 81-155（v1.2 由 1-15 全量扩展）。
- **运行时状态**：`state = { level, levelIdx, w, h, walls:{idx:true}, goals:{idx:true}, boxes:{} , player:idx, face:0, cmds:[{id,rep}], prog, execDone, won, ... }`
  - `face` 朝右 0/下 1/左 2/上 3（`DIRS = [[1,0],[0,1],[-1,0],[0,-1]]`）
  - idx = `y*w + x`，walls/goals/boxes 为哈希集对象
- **存储**（V0.4 迁移）：`LX_SHARED.storage.configure({toolName:'jiqirenzoumi'})` → 键 `redtools.jiqirenzoumi.v1`；
  结构 `{ version, best:{easy,normal,hard}, recent:{...}, checkin:{dates,streak}, history:[] }`。

### 4.2 执行器（编程核心，v1.3）

```
execRun(): 已执行/已通关/空序列 → return；loadLevelState() 重置盘面；清 _loopLeft；execStep()
execStep(): prog ≥ cmds.length → execDone + checkWin()
  cmd._loopLeft === undefined ? 初始化为 cmd.rep||1    # ⚠️ 修复：不能 !x 判断（0 会误重置死循环）
  _loopLeft ≤ 0 → prog+1 进入下一条
  _loopLeft -= 1 → 执行本条：
    left:  face=(face+3)%4   right: face=(face+1)%4
    fwd/block: 前向格 (nr,nc)：
      block: 前方越界/墙/箱子 → 不动（条件成立）；否则 player 前进一格
      fwd:   前方墙 → 不动；箱 → 能推则推（推箱同推箱子）；空地 → 前进
  renderBoardOnly() + setTimeout(260ms) 步进
```

- **循环计数修复（v1.3）**：初版 `if (!cmd._loopLeft) { cmd._loopLeft = cmd.rep||1; }`
  在 `_loopLeft=0` 时 `!0=true` 会反复重置为 rep → **单步指令死循环**（每 260ms 重复执行）。
  改判 `=== undefined` 且 `execRun` 清零用 `undefined`（不能用 0，0 会被当作已执行完跳过）。
- **block 语义落地（v1.3）**：复用 fwd 的方位/边界计算，但**不推箱**——前方有箱视为障碍停步。

### 4.3 渲染

- SVG `board-svg`（viewBox 0 0 w*32 h*32）：墙 rect / 地板 rect / 目标 circle / 箱 rect 双层 / 玩家蓝圆+白眼。
- 执行中每步 `renderBoardOnly()` 重建（≤13×15=195 格，开销可忽略）。

### 4.4 判定/星级

- **胜利**：所有箱子在目标集合（箱数=目标数由关卡保证）。
- **星级**（实际指令步数 vs 最优推动数 b，扁平宽松）：
  - b≠null：steps ≤ b×1.5 → 3★；≤ b×2.5 → 2★；通关 1★
  - b=null（19 关求解超时）：通关 2 星封顶
  - 重复通关仅当星级更高或推进更远才覆盖 best
- **打卡**：通关一关写入今日（去重、365 天滚动）、重算连续天数（对齐 `docs/通用需求-打卡分享记录.md`）。

## 5. 合规自检（Chrome 61 / 容器规范）

- JS：ES2017 经典脚本（var/function），无 `?.`/`??`/对象展开/module/import/内联事件/eval/fetch/XHR/Worker。
- CSS：无 `gap`/`aspect-ratio`/`clamp()`/逻辑属性/`:has`；flex 兼容写法；无外部 URL。
- 资源：SVG 程序化渲染零素材；无 CSP meta（容器管理）。
- 包体：zip ≈ 0.02 MiB（≪ 2 MiB 建议值）。
- 存储：LX_SHARED.storage 本地键，不联网上传；无追踪/无广告/无内购。

## 6. 测试记录（2026-09-22）

| 项 | 结果 |
|---|---|
| 构建 `build_all.py --tool 机器人走迷宫 --mode offline/online` | ✅ 成功（v1.2 起双模式） |
| 审计 `audit_artifact.py`（dist + zip） | ✅ PASS：7 files 0 warning |
| 合规自查（红线禁词/API/外链） | ✅ 通过 |
| v1.0 冒烟 | ✅ 首页/关卡/指令序列/执行/判定/打卡 |
| v1.1 冒烟 | ✅ 三档区段全量（1-30/31-80/81-155）+ 循环 ×2/×3 执行正确 |
| v1.2 冒烟 | ✅ 关卡全量开放回归 + 19 关 b=null 兜底 2★ |
| **v1.3 block 全场景冒烟**（Playwright file:// + 真实 UI 驱动） | ✅ **5/5 PASS + JS 错误 0**：S1 block×1 空地前进（20→21）/ S2 block×3 循环（20→22，第 3 步墙前停）/ S3 墙前 block 不动（22 保持）/ S4 箱前 block 不动（20 保持，朝下遇箱）/ S5 fwd×3 循环回归（22 保持，撞墙停） |
| v1.3 执行器单测 | ✅ `_loopLeft` 死循环修复（undefined 判初始化）+ 清零改 undefined；复测 prog 推进/execDone 正常 |
| 视觉质检 | ⚠️ 本轮未做（逻辑改动无视觉变化；前一版本已目检） |
| Chrome 61 / Android 8.1 / 真机 WebView | ⚠️ 未实测（如实标记） |

## 7. 变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-22 | D1 原型首版：指令序列编程（↑/↰/↱）+ 目标盘面判定 + 星级（指令数 vs 最优 b）+ 简单 1-15 关；复用推箱子 levels.js/解析/推箱逻辑 |
| v1.1 | 2026-09-22 | 循环指令 chip ×2/×3/×4（单击递增 1→2→3→4→1）；难度区段扩展（easy 1-30 / normal 31-80 / hard 81-155） |
| v1.2 | 2026-09-22 | 关卡全量开放：155 关（19 关 b=null 兜底 2★）；条件指令评估（完整 if/else 低龄认知暂缓，候选「避障守卫」） |
| v1.3 | 2026-09-22 | **条件指令「🧱 前方探测」**：前方墙/箱/边界 → 不动；空地 → 前进一格；**修复 `_loopLeft` 死循环 bug**（undefined 判初始化 + execRun 清零改 undefined）；概念路线「顺序 → 循环 → 条件」齐备 |

## 8. 与系列规范的关系

- **系列归属**：编程教学子系列首款（D1），目录 `series/益智/编程教学/`；立项依据
  `docs/教育工具/编程教学产品线-立项评估.md`（推迟款转型映射表）。
- **可复用核心**：盘面渲染/星级/打卡/持久化骨架对齐 `series/益智/设计文档.md`；
  关卡数据/解析/推箱逻辑**复用自推箱子**（`levels.js` 完整拷贝 + 主逻辑内联推箱判定）。
- **合规先例**：经典游戏 4 款 + 测速 2 款推迟后，本工具是「游戏类 → 编程教学」转型的
  首个有标准答案可立项形态（指导原则 §一之二）。

## 9. 概念教学路线（后续规划）

- ✅ 顺序（↑↰↱，v1.0）→ ✅ 循环（chip ×N，v1.1）→ ✅ 条件（🧱 前方探测，v1.3）→
- 后续候选：正式 if/else 分支（前方探测结果决策）、参数化移动（走 N 步）、嵌套循环、
  关卡编辑器（孩子自建迷宫题）。