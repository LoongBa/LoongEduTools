# 数学口算（学科系列）— 设计文档

> 状态：**✅ v1.0 已构建发布，P0 + P1 全部实现（2026-09-18 smoke 全部通过）**。
> 本文档由原《需求分析与设计方案》修订而来：标题基调改为「设计文档」，并按 `main.js`
> 实际实现修正过期状态标注（2026-09-18）。功能事实以源码为准。
> 关联：产品矩阵规划 P0「口算每日打卡」；系列设计规范见 `series/学科/设计文档.md`。
> 调研依据：小猿口算（小猿AI）/作业帮口算/学而思口算/宝宝巴士数学功能对比（2026-09）。
> 更新：2026-09-17（v2 采纳 Oracle 评审）；2026-09-18（实现状态修订）

---

## 1. 产品定位

| 项 | 说明 |
|---|---|
| 工具名 | 数学口算 |
| 系列 | 学科 |
| 对标 | 小猿口算（功能标杆）、宝宝巴士数学（低龄交互）、叫叫口算（激励体系） |
| 形态 | 小红书小工具（离线 zip + H5，Chrome 61 基线，无网络） |
| 目标用户 | 1-6 年级小学生（家长陪练/自习） |
| 核心价值 | **离线可用的口算练习 + 每日打卡 + 分层知识点** |

**差异化机会**（主流 app 普遍需联网）：
1. 完全离线可用（zip 内所有数据）✅
2. 本地段位 + 历史最佳榜（替代在线排行榜）✅ 已实现
3. 简洁无游戏化负担的激励（家长友好）✅

## 2. 年级与知识点分级体系（v2：按"口算能力子集"修订）

> 依据：课标 2022 + 人教版教材目录 + Oracle 评审修正。
> **核心原则：知识点 = 该单元中适合口算的子集，而非笼统单元名**。
> **三年级是分水岭**（整数→分数、具体→抽象过渡）。

| 年级 | 口算知识点（id） | 口算子集边界（明确题型范围） |
|---|---|---|
| **一年级** | g1_10addsub / g1_20add / g1_20sub / g1_100 | 10 以内加减（分与合/连加连减）；20 以内进位加（凑十法）；20 以内退位减（破十法）；整十数加减、两位数±一位数/整十数（不进位/不退位） |
| **二年级** | g2_mult / g2_div / g2_100 / g2_mixed | 表内乘法（2-9 口诀）；表内除法（口诀求商）；100 以内加减（进位/退位）；混合运算（先乘除后加减/小括号）、有余数除法 |
| **三年级** | g3_wan / g3_mult1 / g3_mult2 / g3_div1 / g3_frac | 万以内加减（几百几十/验算）；整十整百×一位数；**两位数×两位数（口算子集：整十数乘两位数）**；一位数除法（口算/有余数/0 除）；同分母分数加减、1 减几分之几 |
| **四年级** | g4_big / g4_simple / g4_div2 / g4_dec | 亿以内大数（读写/近似数/改写）；**运算定律简便计算（25×4×8、125×8、凑整）**；整十数÷整十数口算、试商；小数读写比较、一位小数加减 |
| **五年级** | g5_decmul / g5_decdiv / g5_frac1 / g5_frac2 | 小数×整数/小数×小数（口算）；小数÷整数/小数÷小数（口算）；约分通分/分数小数互化；异分母分数加减 |
| **六年级** | g6_fracmul / g6_fracdiv / g6_pct / g6_ratio | 分数×分数/分数四则混合口算；倒数/分数÷分数；**百分数互化（25%↔1/4↔0.25）**；**化简比/求比值（不含解比例）** |

**v2 修订说明（对照 Oracle 意见）**：
- 三年级补充 `g3_mult2`（两位数乘两位数口算子集）✅
- 四年级 `g4_mult2`（三位数乘两位数，笔算内容）→ **移除**，改为 `g4_simple`（运算定律简便计算）✅
- 六年级 `g6_ratio` 限定**化简比/求比值**，**排除解比例**（方程笔算非口算）✅
- 六年级 `g6_pct` 限定**互化口算**，不含百分数应用题 ✅

**实现状态（2026-09-18）**：25 个知识点与 `main.js` POINTS 完全一致
（一年级 4 / 二年级 4 / 三年级 5 / 四年级 4 / 五年级 4 / 六年级 4），v2 修订全部落地。

**难度分层**（与年级解耦 4 级，供挑战模式复用）：
- 入门（10 以内）→ 基础（20 以内/表内乘除）→ 提高（100 以内/多位数）→ 挑战（大数/小数/分数混合）

> 注：挑战场景已落地为计时挑战（30/60/120 秒），难度分层未单独成级。

## 3. 功能需求

### 3.1 核心功能（P0）

| 编号 | 功能 | 说明 | 对标 | 状态 |
|---|---|---|---|---|
| F1 | **年级+知识点选题** | 首页选年级 → 选知识点 → 选模式进入练习 | 小猿口算 | ✅ 已实现 |
| F2 | **随机出题** | 每次随机生成，同配置结果不同；普通练习固定 10 题，计时挑战固定 20 题 | 小猿口算 | ✅ 已实现 |
| F3 | **点选作答** | **纯 `<button>` 数字键盘**（禁用 `<input type=number>` 避免 IME 冲突）；可删除/提交 | 叫叫口算 | ✅ 已实现 |
| F4 | **逐题即时反馈** | 答对绿✓+连击，答错红✗+显示正确答案 | 练学宝 | ✅ 已实现 |
| F5 | **计时挑战** | 30/60/120 秒限时；`performance.now()` + **`visibilitychange` 后台自动暂停**；倒计时+成绩 | 练学宝 | ✅ 已实现 |
| F6 | **每日打卡** | 日历火苗点亮 + 连续天数；完成练习即打卡 | 叫叫口算 | ✅ 已实现 |
| F7 | **错题本** | localStorage 持久化；**上限 500 条 FIFO 淘汰**；连续答对 3 次同类型移除 | 叫叫口算 | ✅ 已实现 |

**实现说明（2026-09-18，对照 main.js）**：
- **F2**：原设计「题量 10/20/30/50 可选」未实现；实际为普通练习 10 题、计时挑战 20 题。
- **F5**：点击知识点后弹窗 `promptTimerMode`，可选「普通练习（10 题）/ ⏱30 秒挑战 / ⏱60 秒挑战 / ⏱120 秒挑战」；
  计时模式 20 题；`setInterval(100ms)` 驱动倒计时，剩余 ≤10 秒计时变红（danger），时间到自动交卷 `finishQuiz`。
- **F7**：错题本仅记录/展示（题目 + 答案 + 错 N 次）；原设计「下次练习优先重练」未实现（不参与出题权重）。

### 3.2 激励体系（v2 精简）

| 编号 | 功能 | 优先级 | 说明 | 状态 |
|---|---|---|---|---|
| F8 | **连击** | P0 | 即时反馈核心（答对连击计数，≥2 显示 🔥） | ✅ 已实现 |
| F9 | **每日打卡** | P0 | 习惯养成（与 F6 同一系统） | ✅ 已实现 |
| F10 | **星星制** | P0 | 全对 3 星 / ≥80% 2 星 / ≥60% 1 星，记录在知识点（「已获 N ★」） | ✅ 已实现 |
| F11 | **成就系统** | P1 | **勋章墙 + 本地段位合并为单一「成就」**（里程碑勋章 + 累计答对数升级段位：青铜→王者） | ✅ 已实现（2026-09-18） |
| F12 | **结果页** | P1 | 成绩报告精简：正确率/答对数/用时/最大连击/星星 | ✅ 已实现（2026-09-18） |

**v2 精简说明（对照 Oracle 意见）**：
- ❌ 移除：独立"勋章墙 + 段位"双系统 → 合并为单一「成就」✅
- ⏬ 降级：双人对战 → **P2 后置**（离线场景使用率低，ROI 低）✅
- 保留 P0：连击 + 打卡 + 星星（不过度设计，家长友好）

**实现说明（2026-09-18，对照 main.js）**：
- **F10**：原设计「全对 3 星解锁下一关」未实现关卡解锁；星级仅记录于知识点，
  知识点列表显示「已获 N ★ / 未练习」。
- **F11**：段位按累计答对数 `calcTier` 实时计算（青铜 0 / 白银 50 / 黄金 200 / 钻石 500 /
  星耀 1000 / 王者 2000）；勋章墙 7 枚（初学者/百题斩/千题王/全对达人/连击大师/打卡 7 天/打卡 30 天）；
  成就页含「距下一段位还需 N 题」提示与历史最佳展示。
- **F12**：原设计「错题数/薄弱知识点」未实现；结果页展示正确率/答对数/用时/最大连击/星星。

### 3.3 裁剪说明（离线约束）

| 主流功能 | 处理 | 状态 |
|---|---|---|
| 在线 PK/排行榜 | ❌ → 本地段位 + 历史最佳榜 | ✅ 已实现 |
| 手写 + OCR 批改 | ❌ → 点选作答（`<button>` 数字键盘） | ✅ 已实现 |
| AI 薄弱点诊断 | ⚠️ 错题本记录/展示（连续答对 3 次移除、上限 500 FIFO）；「薄弱优先」出题未实现 | ⚠️ 部分 |
| 班级/教师端 | ❌ 不做 | — |
| 打印题卡 | ⚠️ `window.print()`（P2） | ⏳ P2 后置 |
| 教材版本 | ⚠️ 先人教版，留接口（P2） | ⏳ P2 后置 |

## 4. 出题引擎设计（v2：混合方案 + 反推校验）

> 实现状态（2026-09-18）：已落地于 `generators.js`（25 个生成器 + `validate()` 反推校验）。

### 4.1 生成策略（混合）

| 知识点类型 | 方案 | 理由 | 状态 |
|---|---|---|---|
| 简单四则（加减乘除/表内/整十数） | **运行时纯函数生成器** | 算法简单、正确性可控、零体积 | ✅ 已实现 |
| 复杂知识点（异分母分数/通分/小数除/百分数互化） | **参数化模板**（题干模板 + 受约束参数，如分母∈{2,3,4,5,6,8,10}） | 保证正确性，体积小 | ✅ 已实现 |

### 4.2 反推校验（P0 强制）

- **每个生成器配反推校验函数**：生成题目后用数学运算反向验算答案，校验失败则重新生成。
  ✅ 已实现（`generators.js` `validate()`：正向 7500/7500 通过、负向篡改 7500/7500 被拒，
  回归命令见交接文档「快速验证命令」）
- 理由：离线小工具不可远程修复，生成器 bug 产出错误题目是**致命伤**，必须生成期自校验。
- 除法保证整除性、分数保证约分后最简、进位/退位按知识点配置过滤。✅

### 4.3 出题规则

| 规则 | 实现 | 状态 |
|---|---|---|
| 随机范围 | 每题随机参数（如 20 以内进位加：和 ≤20 且需进位） | ✅ 已实现 |
| 难度过滤 | 按知识点配置范围 | ✅ 已实现 |
| 薄弱优先 | 原设计：错题本薄弱提高权重 → **未实现**（错题本仅记录/展示） | ❌ 未实现 |
| 防重复 | 原设计：同一次练习内不连续出同一题 → **未实现**（随机生成，同题可能重复） | ❌ 未实现 |

### 4.4 题目结构

```js
{ type: "g1_20add", text: "9 + 6 =", answer: 15, check: fn(answer) }
// check 为反推校验函数（或生成期已校验，运行时仅存 answer）
```

## 5. 技术方案

### 5.1 数据驱动架构（复用 build_framework）

```
series/学科/数学口算/
├── docs/                        # 文档（设计文档/使用文档/交接文档）
├── src/                        # 前端静态资源
│   ├── index.html
│   └── assets/
│       ├── main.js             # 口算逻辑（选题/练习/打卡/错题本/成就/家长面板）
│       ├── style.css           # Chrome 61 基线样式
│       ├── icon_base.png       # 系列公共图标母版（构建时叠加角标）
│       └── generators.js       # 25 个知识点出题生成器（纯函数 + 反推校验）
└── README.md
```

- 无 book.json 数据源：知识点/题目全部内置前端。
- `tools.py` 登记 ToolConfig；`build_framework.py` 复用 copy_static / make_icon / package_zip，
  跳过 convert_images / convert_audio（无图片素材、无音频）。✅

### 5.2 听算（P2，明确降级）

- **开发前必须先真机验证** Android 8.1 WebView 下中文 speechSynthesis 可用性
  （重点测华为/小米/OPPO 低端机——部分国产设备阉割或用户禁用 TTS）。
- 降级路径明确：**TTS 不可用时自动隐藏听算入口，不报错**。
- 听算定位为**增值功能**，非核心（F4 点选是核心）。

### 5.3 持久化（localStorage，v2 修订）

```js
{
  version: 1,                          // ★ 版本字段 + 迁移预留
  profile: {
    totalCorrect,                      // 累计答对数（段位依据）
    maxCombo,                          // 历史最大连击
    tier,                              // 当前段位（青铜→王者）
    badges: [],                        // 已解锁勋章 id
    best: { rate, combo, elapsedMs },  // 历史最佳（正确率/最大连击/最快用时）
    stars: { pointId: n }              // 各知识点星级
  },
  checkin: { dates: ["YYYYMMDD"], streak },   // 打卡日期滚动 365 天
  wrongBook: [ {type, text, answer, wrongCount, correctStreak, lastTime} ],  // ★ 上限 500 FIFO
  history: [ {date, point, rate, count, correct, elapsedMs} ]                // ★ 滚动 30 条
}
```

**v2 修订**：加 `version` 字段 + 迁移函数；错题本上限 500（FIFO）；历史滚动 30 天 ✅
**实现说明（2026-09-18）**：存储 key 为 `redtools.math.v1`；原设计 `profile.segment` 未采用，
段位改由 `totalCorrect` + `calcTier` 实时计算；成就/历史最佳/家长面板统计均读自该结构。

### 5.4 Chrome 61 / 交互合规（v2 补强）

- **数字键盘**：`<button>` 实现，禁用 `<input type=number>`（避免 Android 软键盘/IME 冲突）✅
- **计时**：`performance.now()` + `visibilitychange` 后台暂停（后台标签页 Date.now 被节流）✅
- **分数渲染**：HTML+CSS 上下排列（分子/分母），不用图片；无竖式（口算无竖式）✅
- **触控**：按钮 hit area ≥48×48px（小学生手指粗）；反馈动画避免强闪烁（光敏风险）✅
- **家长面板入口**：标题 5 秒内连点 5 次（touchstart/mousedown 计数）✅ 已实现
- **CSS**：禁用 gap/aspect-ratio/clamp/:has/container queries；CSS 变量、flex 基础 OK ✅
- **首屏**：≤1 秒渲染，数据 localStorage 预加载，无"加载中"等待 ✅
- JS：ES2017 经典脚本（无 module/import/export）；无网络请求/外部资源/Worker/eval

## 6. 交互流程

```
首页 ──► 选年级（1-6）──► 选知识点 ──► 模式弹窗
                                          │  普通练习（10 题）/ ⏱30 秒 / ⏱60 秒 / ⏱120 秒挑战
                                          ▼
                                        练习页
                                          ├─ 进度「第 x / N 题」+ 计时（挑战模式）
                                          ├─ 连击 🔥（≥2 显示）
                                          ├─ 题目卡片 + 作答区
                                          ├─ 数字键盘（1-9、.、0、⌫、✓）
                                          ├─ 即时反馈：答对绿✓（600ms 下一题）/ 答错红✗+正确答案（1400ms 下一题）
                                          ├─ 计时挑战：≤10s 计时变红；切后台自动暂停；时间到自动交卷
                                          └─ 完成 → 结果页（正确率/答对数/用时/最大连击/星星）
                                                  └─ 打卡（完成即点亮今日火苗）
                                                   └─ 再来一组 / 返回知识点
首页底部导航：打卡日历 / 错题本 / 成就 / 首页
练习中底部导航：「退出练习」返回知识点列表
```

## 7. 验收标准

| 项 | 标准 | 状态（2026-09-18） |
|---|---|---|
| 知识点覆盖 | 1-6 年级（三年级 5 个、其余 4 个/年级，共 25），每生成器 ≥50 道不重复题 | ✅ 25 个知识点落地 |
| 正确性 | **每个生成器反推校验函数验证**（进位/退位/整除/约分边界） | ✅ validate() 双向 7500/7500 |
| 打卡 | 连续天数计算正确（跨天/缺天） | ✅ calcStreak + smoke 通过 |
| 错题本 | 记录、连续答对 3 次移除、上限 500 FIFO | ✅ 代码完整 |
| 激励 | 连击/打卡/星星（P0）+ 成就系统（P1）闭环 | ✅ 全部闭环 |
| 体积 | zip ≤ 2 MiB（纯前端无音频） | ✅ 实测 0.03 MiB |
| 合规 | 审计 PASS + Chrome 61 自查 + 冒烟测试 | ✅ 审计 6 files 0 warning + smoke 通过 |
| 交互 | 数字键盘无 IME 冲突；计时后台暂停；按钮 ≥48px | ✅ smoke 覆盖 |

## 8. 实施计划

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | src 骨架 + 选题页 + 练习页（button 键盘/即时反馈/随机出题/反推校验） | ✅ 已完成 |
| 2 | 25 知识点生成器 + 反推校验（边界用例） | ✅ 已完成（validate 双向验证） |
| 3 | 打卡 + 错题本（上限/淘汰）+ 连击/星星 + 结果页 | ✅ 已完成 |
| 4 | 成就系统（勋章+段位）+ 历史最佳 + 家长面板 + 打磨 | ✅ 已完成（2026-09-18） |
| P2 后置 | 双人对战 / 打印 / 听算（先真机验证 TTS）/ 北师大苏教版 | ⏳ 后置 |

## 9. Oracle 审核记录

- **结论**：✅ 可进入开发（需先完成 P0 修改项，已全部落实）
- **P0 落实**：① 知识点分级重排（三年级补两位数×、四年级改运算定律、六年级限化简比/互化）✅
  ② 出题混合方案 + 反推校验 ✅ ③ 激励精简（成就合并、双人对战降 P2）✅
  ④ localStorage version + 错题本 500 + 历史 30 天 ✅ ⑤ 听算 TTS 真机验证 + 降级隐藏 ✅
- **P1 采纳**：家长面板（标题 5 秒内连点 5 次隐藏入口，2026-09-18 已实现）、数字键盘 button 实现、
  计时后台暂停、按钮 ≥48px、反馈动画温和 ✅
