# 数独思维 — 工具说明

> 益智系列 · 逻辑思维子系列第 4 款（P1 数独思维，产品矩阵 §3.4，对标洪恩数独）。
> 适用 K1-3–K4-6（L2–L3）。数字符号，v1.17（📅 每日挑战难度轮换（周1/2 简单→周3-5 普通→周6 困难→周日普通）+ 🏅 成就系统 8 枚（+闯关地图通关/月度坚持）+ 📊 家长报告成长进度卡（每日挑战/成就/闯关进度，全派生）+ ⚙️ 设置面板（音效开关/清除本地进度/关于）+ 🗺️ 闯关地图（3 档 × 3 关线性推进，同关同题）+ 📅 每日挑战（当日种子同题）+ 分享题二维码（微信长按识别导入）+ 双指缩放棋盘 + 🧩 自由解题进阶技巧（唯一余数 / X-Wing，独立徽章不吹适龄）+ E5 连败降档建议 + 📊 家长报告 + 9×9 小屏适配 + 交叉排除技巧教学关（L4·四技巧全齐）+ 分享成绩 + 分享题/导入 + 错题本/收藏本 + 提示带讲解限流 + 技巧徽章墙 + 规则教学三步 + 候选笔记铅笔模式 + 断局恢复；无图标版）。
> 设计/使用文档见 `docs/教育小程序/益智/逻辑思维/数独/数独思维-{设计,使用}文档.md`。

## 玩法

经典数独：点选空格 → 底部数字条点数字填入，目标让每行 / 每列 / 每宫（粗线框）都只出现一次 1~N。
全部填满且无冲突即过关。**每次进入随机生成全新盘面**（随机挖洞，保证唯一解）。

- 三档难度（双参数：盘面尺寸 × 目标已知格）：

  | key | 名称 | 盘面 | 目标已知格 | 约挖空 |
  |-----|------|------|-----------|--------|
  | 4 | 简单 | 4×4 | 10 | ~6 空 |
  | 6 | 普通 | 6×6 | 21 | ~15 空 |
  | 9 | 困难 | 9×9 | 33 | ~48 空 |

  （生成器实际已知格落在 [目标, 目标+4]，顶栏显示实际值。）

- 输入：点选格子 + 底部数字条（1..N 按钮 ≥48px）；实体键盘 1-9 / Backspace / Delete 增强
- **规则教学（v1.3）**：难度页「📖 规则教学」入口 → 4×4 演示盘三步讲解（行 → 列 → 宫不重复），每步高亮对应约束格 + 引导填空，末步「开始挑战」回难度页；可上一步/跳过
- **技巧教学关（v1.4/v1.5）**：难度页技巧入口（单宫排除 / 行列排除 / 区块排除）→ 独立教学关（4×4 简化盘，逐步引导落子 + 讲解），完成点亮技巧徽章（localStorage `store.skills`）；独立标记不计普通进度、可跳过、可重练；**技巧徽章墙（v1.5）**：难度页「🏆 技巧徽章墙」入口 → 展示全部技巧徽章点亮状态（✅ 已点亮 / 🎯 可学习 / 🔒 即将上线）
- **自由解题（v1.10）**：难度页「🧩 自由解题」入口 → 进阶技巧教学关（唯一余数 / X-Wing，6×6 盘面逐步引导），完成点亮**独立进阶徽章**（`store.advSkills`，不吹适龄徽章、不计入徽章墙 X/4 与家长报告掌握度）
- **每日挑战（v1.12/v1.17）**：难度页「📅 每日挑战」入口 → **难度周轮换（v1.17）**：周一/周二 4×4 简单 → 周三至周五 6×6 普通 → 周六 9×9 周末挑战 → 周日 6×6 收尾（`DAILY_DIFFS` 按 `getDay()`）；当日种子（YYYYMMDD）生成**每日同题**（同一天人人同题；跨天换题；solver.js 零改动，main.js 层临时替换 Math.random）；计入普通成绩与打卡，顶栏「· 每日题」标记；通关记 `store.daily = {date, level}`（记录实际难度），次日换新题
- **闯关地图（v1.13）**：难度页「🗺️ 闯关地图」入口 → 3 档难度 × 每档 3 关 = 9 关线性推进（4×4 → 6×6 → 9×9）；每关固定 seed（`MAP_LEVELS.seedBase`，复用 mulberry32）→ **同关同题**可复玩；解锁规则 `i < completed.length` 已通 / `i === completed.length` 可玩 / 其余锁定（温和提示）；通关记 `store.mapProgress.completed`（按序 push 防重复），顶栏「· 关卡 X-Y」标记，全部通关显示 🎉 横幅；计入普通成绩与打卡，无竞技无排行；solver.js 零改动
- **设置面板（v1.14）**：难度页「⚙️ 设置」入口 → 音效开关（`store.settings.sound`，tone 总出口拦截全音效）/ 清除本地进度（确认浮层 → `localStorage.removeItem` + reload 重建默认；清空成绩/打卡/错题/成就/闯关等）/ 关于（版本 + 隐私说明「无账号无云同步，数据只存本机」）
- **成就系统（v1.12/v1.17）**：难度页「🏅 成就」入口 → 成就墙（**8 枚（v1.17 扩 2）**个人里程碑：每日挑战首通 / 连续打卡 3·7 天 / 十局小达人 / 技巧大师 / 三星完美 / **闯关地图通关 / 月度坚持（连续打卡 30 天）**），通关结算自动解锁并在结算浮层「🏅 解锁成就」提示；无竞技无排行
- **E5 自适应难度（v1.10）**：最近两局「吃力」（错误+提示 ≥ 该档半数空格）→ 结算浮层 + 难度页温和提示建议降档（不扣分）；进入任一局即清除建议
- 反馈：合法落子绿色确认（答错不惩罚，静默接受）；冲突红闪 400ms + 温和提示，错误计数 +1
- 教具高亮：选中格橙色描边，同行/列/宫浅蓝 tint，同数字格子浅橙 tint
- 工具：橡皮（清选中格，含候选笔记）/ 撤销（恢复上一步，含提示/橡皮/笔记均可撤）/ 提示（自动找候选最少格填正确解）/ **✏ 铅笔模式（候选笔记）**：切换后点数字在格子里记候选数，再点取消；行/列/宫已有该数时拒绝并温和提示
- 自动铅笔：普通或提示落定某数后，同行/列/宫其他格的该候选数自动清除（持续同步约束）
- 星级：0 提示 0 错 → 3★；提示 ≤1 且 错 ≤3 → 2★；其余 1★（用提示星级封顶 2★）；笔记不影响星级
- 计时：游戏页渲染起算（performance.now + setInterval 100ms，⏱ 07.5 格式），整局计时
- **断局恢复（v1.2）**：每步操作自动存对局快照（盘面/候选笔记/撤销栈/提示/错误/计时）；中途退出后难度页显示「▶ 继续上次」，续玩时计时从保存点续算；通关或重开后快照清除
- 进度：各难度最佳（星级高优先，同星级比用时）/ 最近用时 / 成绩记录 / 每日打卡
  （localStorage `redtools.shudurumen.v1`）

## 算法

`src/assets/solver.js` 内 `window.SUDOKU`，移植自 `docs/教育小程序/益智/逻辑思维/数独/数独小侦探原型/logic_test.js`
（其自身 node 测试已断言唯一解 / 已知格范围 / 解合法性），并降级为 ES2017（var + function）：

- `boxDims(N)`：宫尺寸 4→[2,2]、6→[2,3]、9→[3,3]
- `cellValid`：行/列/宫冲突判定
- `fillBoard(N)`：随机回溯完整解
- `solveCount(board,N,limit)`：解计数（唯一解判定）
- `genPuzzle(N,givens)`：从完整解随机挖洞，每次挖洞校验唯一解，多轮尝试逼近目标已知格
- `getCandidates` / `findHintCell`：候选few格 / 提示选格

## 目录

```
数独思维/
├── src/
│   ├── index.html          # 入口（data.js → solver.js → main.js）
│   └── assets/
│       ├── solver.js       # 数独核心算法（window.SUDOKU，纯算法无 DOM）
│       ├── main.js         # 游戏逻辑（棋盘/交互/成绩打卡，ES2017 经典脚本）
│       ├── style.css       # 样式（Chrome 61 基线，天蓝系）
│       └── icon_base.png   # 图标母版（构建叠加角标）
└── README.md               # 本文件
```

纯代码 + 母版图标，**无外部图片/音频/网络资源**，包体极小。

## 构建与测试

```bash
python build_all.py --tool 数独思维
python .skill/minitool-zip-builder/scripts/audit_artifact.py dist/益智/数独思维
# 算法单测：node C:\Users\coffe\AppData\Local\Temp\opencode\pw-smoke\test_sudoku.js
```

## 变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.17 | 2026-09-25 | **每日挑战难度轮换 + 成就扩 2 枚**：`DAILY_DIFFS = ['6','4','4','6','6','6','9']` 按星期轮换（周1/2 简单 4×4 → 周3-5 普通 6×6 → 周6 周末挑战 9×9 → 周日 6×6 收尾）；`startDaily()` 用当日难度 + 当日种子同题，难度页每日卡 sub 动态显示今日难度（`今日一题 · 简单 4×4 · 和全世界同题`），`store.daily.level` 记录实际难度；ACHIEVEMENTS 6→8 枚（+「闯关地图通关」mapMaster 满 9 关 /「月度坚持」monthStreak 连续打卡 30 天），计数/视图/成长卡自动 X/8；solver.js 零改动 |
| v1.16 | 2026-09-25 | **家长报告成长进度卡**：报告新增「📈 成长进度」卡（卡片 C，`report-card`/`report-row` 复用）——每日挑战（今日完成 ✅ / 未完成 🎯 + 连续打卡 N 天）、成就（已解锁 X / 6 枚 + 最近解锁名，取 `store.achievements` 日期最大者）、闯关地图（通关 X / 9 关 + 当前进度关与档位，全通 🎉）；**全部数据派生**自 `store.daily/checkin/achievements/mapProgress`，零新 schema 零迁移，只读不写；solver.js 零改动 |
| v1.15 | 2026-09-25 | **更名「数独思维」**：教育语境避免「侦探」游戏感误会；目录/文档/入口/产物名全量 `git mv` 同步（series `数独思维/`、docs 3 份、tools.py 条目）；成就「十局小侦探」→「十局小达人」、闯关 3-3「大侦探！」→「思维高手」称号中性化；solver.js 零改动 |
| v1.14 | 2026-09-25 | **设置面板**：难度页「⚙️ 设置」→ 音效开关（`store.settings.sound` 持久化，模块级 `soundEnabled` + `tone()` 总出口拦截，开时播确认音）/ 清除本地进度（确认浮层「🗑️ 确定清除？」→ `localStorage.removeItem(STORE_KEY)` + reload 重建默认 store）/ 关于（版本 + 「无账号无云同步，数据只存本机」）；零迁移（旧存档补默认 `settings:{sound:true}`）；solver.js 零改动 |
| v1.13 | 2026-09-25 | **闯关地图（X1）**：难度页「🗺️ 闯关地图」→ 3 档难度 × 每档 3 关 = 9 关线性推进；每关固定 `seedBase`（复用 mulberry32/newRound(seed)）→ 同关同题可复玩；解锁 `i<completed` 已通 ✅ / `i===completed` 可玩 🔓 / 其余 🔒（toast 温和提示），顶栏「· 关卡 X-Y」，全部通关 🎉 横幅；通关记 `store.mapProgress.completed`（按序 push 防重复），计入普通成绩打卡；solver.js 零改动 |
| v1.12 | 2026-09-25 | **每日挑战 + 成就（X3）**：难度页「📅 每日挑战」→ 固定 6×6 普通，当日种子（`mulberry32` PRNG，`dailySeed()`=YYYYMMDD）生成每日同题（main.js 层临时替换 Math.random，solver.js 零改动）；计入普通成绩打卡、顶栏「· 每日题」、通关记 `store.daily`；「🏅 成就」墙 6 枚个人里程碑（每日挑战首通/连续打卡 3·7 天/十局/技巧大师/三星完美），`checkAchievements()` 通关结算自动解锁 + 结算浮层「🏅 解锁成就」行（`lastUnlocked` 缓存一次）；无竞技无排行；solver.js 零改动 |
| v1.11 | 2026-09-25 | **分享题二维码 + 双指缩放**：分享题浮层新增二维码（qrcode.js MIT 内嵌，UTF-8 编码中文题目文本，微信「长按识别图中二维码 → 复制文本 → 导入」绕开 JS-SDK）；分享成绩提示补充微信长按保存/转发说明；游戏页棋盘双指捏合缩放（E4 P2：scale 0.5-2.2、两指中点 transformOrigin、单指不拦截、`{passive:false}`）+「1:1 复位」按钮（pinchScale>1 显示，新盘自动复位）；solver.js 零改动 |
| v1.10 | 2026-09-25 | **自由解题（进阶技巧教学）+ E5 自适应难度**：SKILLS 新增 2 条进阶条目（唯一余数 uniqueElim / X-Wing xwing，6×6 盘面逐步引导，`group:'adv'`）；完成点亮独立 `store.advSkills`（不吹适龄徽章、徽章墙/家长报告仍 X/4 base）；难度页「🧩 自由解题」入口 + 独立返回路由；E5 连败降档：`suggestLevel()`（最近两局错误+提示 ≥ 半数空格 → 建议降一档），结算浮层 note + 难度页横幅 + 建议档按钮橙色高亮，进入任一局即清除；solver.js 零改动 |
| v1.9 | 2026-09-25 | **家长报告 + 9×9 小屏适配**：难度页「📊 家长报告」（今日反馈：完成局数/平均用时/错误·提示合计/平均星级；近 7 天：局数/总用时/难度分布/技巧掌握度（四徽章点亮状态）——数据全由 store.history+skills 派生，**只读零新 schema**、只存本地不上传）；9×9 小屏适配（竖屏进入一次性「建议横屏」温和提示 + 窄屏（≤340px）字号 16→14px + 棋盘 max-width 420px）；solver.js 零改动 |
| v1.8 | 2026-09-25 | **交叉排除技巧教学关（L4）**：SKILLS 移除 crossElim 锁定占位 → 真实教学关（4×4 盘面 `[0,2,3,0, 3,4,0,0, 2,1,4,3, 0,0,2,1]`，行/列/宫**三源交集**判定——(0,3)=4：(1,4)∩(2,4)∩(1,2,4)；(1,3)=2：(1,2)∩(2,4)∩(1,2,4)，交叉高亮 7 格）；难度页第 4 技巧入口、徽章墙 🔒 → 🎯/✅（四技巧全可学）；solver.js 零改动 |
| v1.7.1 | 2026-09-25 | **分享成绩**：结算浮层「📤 分享成绩」→ canvas 1080×1920 成绩卡（标题/难度/盘面完成缩略（已知格深色+解答天蓝）/星级/用时/错误·提示/已点亮徽章数/日期/落款，长按保存）+ 分享文案复制（textarea+execCommand+toast 兜底）；文案仅展示自身表现（**无对比无排行**，对齐无竞技原则）；solver.js 零改动 |
| v1.7 | 2026-09-25 | **分享题 + 导入**：结算浮层「📤 分享这题」（文本复制 `SD{N}:…` + 打印图片 canvas 黑白大格，题面/答案版切换，长按保存）；难度页「📥 导入题目」（粘贴 → 校验 N/长度/取值/唯一解 → 直接开玩，顶栏「导入题」标记，**导入局不计最佳/打卡**）；solver.js 零改动 |
| v1.6 | 2026-09-25 | **错题本 + 收藏本 + 提示带讲解**：冲突填数/使用提示自动记错题（`store.mistakes`，上限 50 去重，0 错 0 提示重练通关自动消除"掌握即清"）；「⭐ 收藏这局」+ 收藏本（手动加入/移除/清空）；错题重练标注上次填错位置（浅红 replay-mark）；提示升级带讲解（explainHint 判定行/列/宫约束来源 → 高亮该单元 + 文案"已有…缺…"）+ 限流（6×6 每局≤3 / 9×9 每局≤2 / 4×4 不限）；默认 skill 打分不变；solver.js 零改动 |
| v1.5 | 2026-09-25 | **技巧教学关扩展 + 技巧徽章墙**：新增行列排除 / 区块排除教学关（4×4 简化盘逐步引导，行/列/宫高亮教具——修复 v1.4 遗留的 peer/given/selected 类设错元素导致高亮不可见问题，改为两层渲染与普通局同构）；难度页「🏆 技巧徽章墙」入口（✅ 已点亮 / 🎯 可学习 / 🔒 即将上线占位），徽章墙可直达教学关且完成后返回徽章墙；`store.skills` 结构零迁移（新增 key 即生效）；solver.js 零改动 |
| v1.4 | 2026-09-25 | **技巧教学关（单宫排除）**：难度页「🎯 单宫排除技巧」入口；独立教学关（4×4 简化盘逐步引导落子：宫内有 N-1 种数字 → 缺哪个填哪个）；目标格 target 加强 + 宫 peer 高亮；错误温和提示不计错；完成点亮技巧徽章（`store.skills` 持久化）；独立标记不计普通进度、可跳过、可重练；solver.js 零改动 |
| v1.3 | 2026-09-25 | **规则教学三步**：难度页「📖 规则教学」入口 + 4×4 演示盘弹窗（TEACH_BOARD/TEACH_STEPS：行→列→宫不重复，每步高亮约束格+引导填空，末步「开始挑战」）；可上一步/下一步/跳过；复用 overlay 机制，solver.js 零改动 |
| v1.2 | 2026-09-25 | **断局恢复**：每步操作自动存 `store.cur` 对局快照（盘面/候选笔记/撤销栈/提示/错误/累计计时）；难度页「▶ 继续上次」入口（含已玩/已填统计），恢复续算计时；通关/重开清除快照；`saveCur` won 守卫防通关回写 |
| v1.1 | 2026-09-24 | **新增候选笔记（铅笔模式）**：✏ 工具按钮切换铅笔/落子模式；铅笔下点数字记/取消候选，行/列/宫已有该数拒绝并提示；自动铅笔同步（落定或提示后清同行列宫其他格同候选）；橡皮/撤销/提示均兼容笔记；`getCandidates` 数据基础，无 solver.js 改动 |
| v1.0 | 2026-09-19 | 首版：经典数独三档（4×4/6×6/9×9 已知 10/21/33 格，每局随机新盘）+ 点选数字条输入 + 冲突红闪 + 橡皮/撤销/提示 + 教具同数高亮 + 计时星级 + 各难度最佳/打卡 |
