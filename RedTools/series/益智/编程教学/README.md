# 益智 · 编程教学 子系列

> 编程教学线（D1 原型）：把「推迟」的玩法逻辑转型为有标准答案的编程练习（指导原则 §一之二）。
> 定位：指令序列编程启蒙（对标 Scratch 指令序列阶段），纯离线 zip + 有标准答案判定。
> 首个工具：机器人走迷宫（2026-09-22，复用推箱子关卡/解析）。
> 立项依据：`docs/教育工具/编程教学产品线-立项评估.md`。
> 更新：2026-09-22

## 子系列内工具

| 工具 | 状态 | 版本 | 说明 |
|------|------|------|------|
| 机器人走迷宫 | ✅ 已构建 | v1.14 | **指令序列编程**：编排 ↑前进/↰左转/↱右转/**🧱探测**/**➡N步**/**❓if墙**/**🔁循环**/**🧩块** → 执行 → 目标盘面判定；迷宫 = 推箱子关卡（Microban 155 关 + **v1.5 条件分支 20 关（156-175）+ v1.6 参数化 10 关（176-185）+ v1.7 循环块 10 关（186-195）+ v1.8 嵌套循环 10 关（196-205）**，简单 1-30/普通 31-80/挑战 81-205 全量开放）；**编程核心概念：顺序（v1.0）→ 循环（v1.1，chip ×2/×3/×4）→ 条件（v1.3，🧱 前方探测）→ 调试（v1.4，执行高亮+失败定位+差箱提示+执行锁定）→ 分支（v1.5，❓ if前方有墙，then/else 分支 + editCtx 编辑区 + 工作队列执行器 + 嵌套 if ≤2 层）→ 参数化（v1.6，➡ 走N步，一次直线移动 N 格，共享 tryMoveFwd 移动函数）→ 循环块（v1.7，🔁 循环，一组指令重复 N 次，块内可嵌 if/steps 组合控制结构，editCtx type 扩展 + ifDepth 守卫 + execLoop 轮间重置）→ 嵌套循环（v1.8，🔁 内嵌 🔁，二维自动化/嵌套结构，loopDepth 守卫 + body 内嵌 loop + b=嵌套解 totalSteps 星级激励）→ 关卡编辑器（v1.9，孩子自建关卡：12×12 分层涂格墙/箱/目标/玩家 + 可解性 BFS 校验（角死锁剪枝/上限 20 万/箱数 ≤3）+ 本地保存（customLevels，上限 20 关 auto-trim）+ 首页入口 + 自建关挑战 2★ + 已解徽章，创作闭环）→ 抽象/封装（v1.10，🧩 我的块：孩子把重复动作段封装成命名块，主序列 `🧩块名` 引用复用——Scratch My Blocks 启蒙；customBlocks 存储 ≤8 块 × body ≤8 条，块体仅基础指令（防递归），call 星级口径 = 计 1 + body 一次（I-1 防刷分+复用激励），执行器 execCall = execLoop 同构 + 防递归校验（I-2），块编辑即时生效/孤儿灰显兜底，0 新关）→ **块参数（v1.11，🔢 带参数：参数化 steps `steps:null` 距离由调用方传入 1-9——同块不同参数不同行为（函数参数化，Scratch 带输入积木进阶）；B2 hasParam 派生不存储（单一数据源）；execCall 运行时副本（map 浅拷贝 + 参数填值，不同 call 不同参数互不污染，块库永不被写）；totalSteps I1 统一（参数/非参数同走 totalSteps(bd.body)）；N-3 修复（blockListReturn 来源记忆——管理块返回游戏 + 计时衔接，viewHome 兜底 'home'）；附 v1.10 遗留修复（renderGame 补 renderSeq））→ **v1.12 块参数增强**（N1 多处参数化——toggle 开 → 全部 steps 参数化（参数贯穿块内所有步数：方形/拐角模式，execCall 副本已支持同 param 填全部 null，执行零改动）；N2 定义/调用显示分离——定义态 `➡?步` 占位（.param-step）/调用态具体数字；存量 1-param 块不 toggle 保持 mixed 行为不变，toggle off→on 升级 all-param；totalSteps 星级不变（null 计 1））→ **v1.13 混合态+多参数数组**（每步独立参数，Oracle B1/B2 + 8I 修订：参数化 steps 绑定槽位 `paramIdx:0|1`（缺省 #0 兼容 v1.11/12），call `params:[N,M]` 数组存量 `param` 读取兼容零迁移；blockParamCount 派生（max(paramIdx)+1 clamp 2，I3/I4）；编辑器：参数化 chip 单击槽位切换 ?₁⇌?₂、长按 ≥350ms/shift+单击 固定⇌参数双向切换（混合态创建，I1 弃 dblclick + I-new-1/2/3 边界）、toggle I2 幂等（ON 仅未参数化→槽#0 不抹槽位，OFF 全固定）；调用：call chip `🧩名(3,5)` 分段 span data-slot 0/1 独立调参 cycleCallParamSlot（写时归一化 params 删 param）；执行：execCall B2 槽位感知回退 `params[slot] → (slot===0? param||3 : 3)`（槽#1 不继承槽#0 值），B1 深拷贝补 paramIdx；星级零改动（paramIdx 不影响 steps 计 1）——Scratch 多输入积木等价）**；**v1.14 块体含 loop**（循环×抽象组合——块 body 可含 🔁 循环（rep 2-9 + body 基础指令±参数化 steps）；点击 loop chip → 块内「循环编辑态」（次数调参/loop.body 三态交互/添加行仅基础指令 I2 入口隔离）；**参数×循环嵌套**（loop.body 内 ?₁ 步 + params 注入）；**B1 派生递归化**（scanParamSteps 扫顶层+loop.body+if）+ needsCopy=hasParam（递归版，归因参数注入非 _loopLeft 污染）；**B2 resolveStep 提取 / B3 cloneBlockBody 递归深拷贝**；I4 防递归全结构 + I2 单层约束三层；N2 预览 span 视觉标记（固定灰底/参数浅紫/loop 灰框）；星级零改动；**；星级（指令步数 vs 最优解 b，if 计 1+分支，loop 计 1+body 递归，call 计 1+body 一次（v1.10），参数块同口径（v1.11），v1.6/v1.7 新关 b=含 steps/循环块最优编写量）；复用推箱子 levels.js/解析/推箱逻辑零素材。文档 `docs/教育小程序/益智/编程教学/机器人走迷宫-{设计,使用}文档.md` |
| 方块排列指令 | ✅ 已构建 | v1.0 | **构建型布局编程**（与走迷宫移动型互补）：给定目标布局（K 个四格骨牌堆成），为每块编排指令（↻ 旋转 ×N / ⬅➡ 移动 N 格 / ⬇ 落下）→ 执行落定 → 布局逐格比对判定；**概念：顺序（逐块）+ 旋转（空间变换）+ 参数化移动 + 空间规划**；程序化生成器必有解（生成即执行）+ 最优解星级；逐块即时差格反馈（「这一块有 N 格放偏了」）；复用俄罗斯方块 SHAPES 几何零素材。文档 `docs/教育小程序/益智/编程教学/方块排列指令-{设计,使用}文档.md` |
| 合并策略编程 | ✅ 已构建 | v1.0 | **策略型多步规划**（与走迷宫移动型/方块排列构建型互补）：确定性初始盘面（去 2048 随机新块）+ 方向指令序列（⬅➡⬆⬇ 参数化 ×N）→ 执行 → 最终盘面 ≥ 目标数字判定（8/16/32 三档）；**概念：顺序 + 循环（×N）+ 策略规划（多步合并路径预判）**；**BFS 最短指令数星级**（确定性状态空间可算，纯函数 applyMovePure BFS/执行器共用）；生成器必有解（sum≥target 原子量硬约束 + BFS 必达 + seed 兜底）；复用 2048 合并逻辑零素材。文档 `docs/教育小程序/益智/编程教学/合并策略编程-{设计,使用}文档.md` |
| 循环指令 | ✅ 已构建 | v1.0 | **循环结构专训（显式循环块）**：走廊盘面（轨迹格空地 + 相邻非轨迹格墙，堵死捷径唯一路径）+ 机器人 S→G + `🔁 重复 N 次 {一组指令}`（**区别于走迷宫隐式 chip ×N**：走迷宫 = 一个动作做多遍；循环块 = 一组动作做多遍，循环 = 自动化）；**编写量星级**（循环块 1 条 + 内部指令计 1 不乘 N；走廊无捷径 + 展开写必 ≥1.5×optimal 封顶 2★ → 循环块 = 唯一 3★ 激励验证 600/600）；骨架 pattern（fwd×k+right 螺旋无近路）+ simulate 生成器/执行器共用；oracle 修订（走廊堵捷径/自交检查/起点固定/循环块级失败定位）。文档 `docs/教育小程序/益智/编程教学/循环指令-{设计,使用}文档.md` |

## 子系列原则

1. **有标准答案判定**：执行结果盘面 vs 目标盘面（程序可判，非表现性分数）——合规安全区核心。
2. **编程思维培养**：编排 → 执行 → 观察 → 调试（顺序/规划/优化），非直接控制玩法。
3. **复用优先**：关卡/解析/求解基准复用推箱子资产；概念关卡演进（顺序 → 循环 → 条件）。
4. **零素材**：SVG 程序化盘面，符合离线包体目标。
5. **低龄友好渐进**：不一步上完整 if/else → 先用「🧱 前方探测」（单条件判断）入门，
   v1.5 起「❓ if前方有墙」（完整分支结构）结合岔路口关卡渐进（简单单岔 → 挑战嵌套）；
   v1.6 起「➡ 走N步」（参数化移动）结合长直道关卡（参数 vs 循环概念区分）；
   v1.7 起「🔁 循环」（显式循环块）结合循环专关（一组动作自动化，块内可嵌 if/steps 组合）；\n   v1.8 起「🔁 内嵌 🔁」（嵌套循环）结合嵌套专关（二维自动化/嵌套结构，乘法表类比）。

## 规划

- ✅ 顺序指令（v1.0）→ ✅ 循环指令（v1.1）→ ✅ 条件指令「前方探测」（v1.3）→ ✅ 分支指令「if前方有墙」（v1.5）→ ✅ 参数指令「走N步」（v1.6）→ ✅ 循环块「🔁循环」（v1.7）→ ✅ 嵌套循环「🔁内嵌🔁」（v1.8）——**编程核心概念八块闭环完成**
- ✅ 方块排列指令 v1.0（D3，构建型布局编程：旋转 + 参数化移动）
- ✅ 合并策略编程 v1.0（D4，策略型多步规划：BFS 最短解）
- ✅ 循环指令 v1.0（D5，循环结构专训：显式循环块 + 走廊）
- ✅ 走迷宫 v1.9（关卡编辑器：孩子自建迷宫题——12×12 分层涂格 + 可解性 BFS 校验 + 本地保存 + 挑战 2★，**创作闭环**）
- ✅ 走迷宫 v1.10（自定义指令块：孩子把重复动作封装成命名块复用——**抽象/封装概念**，九件套闭环）
- ✅ 走迷宫 v1.11（块参数：🔢 带参数——同块不同参数不同行为——**函数参数化概念**，十件套闭环 + N-3 修复）
- ✅ 走迷宫 v1.12（块参数增强：N1 参数贯穿块内所有步数（方形/拐角）+ N2 定义 `➡?步` 占位/调用数字显示分离）
- ✅ 走迷宫 v1.13（**混合态 + 多参数数组**：每步独立参数——`?₁`/`?₂` 槽位 + call `params:[N,M]` 分段调参 + 长按/shift 固定⇌参数双向切换（混合态）——Scratch 多输入积木等价，高阶参数化闭环）
- ✅ 走迷宫 v1.14（**块体含 loop**：循环藏在块里 + 参数×循环嵌套——循环×抽象组合，控制流抽象渐进闭环）
- 后续候选：块体含 if（条件抽象渐进，未来）、在线服务端（B4，远期）
- 转型候选（矩阵 §10 阶段 D）：✅ 方块排列指令（俄罗斯方块）→ ✅ 合并策略编程（2048）→ ✅ 循环指令（反应测速，**推迟款转型全部完成**）→ ✅ 走迷宫 v1.5（条件分支深化）→ ✅ 走迷宫 v1.6（参数化移动深化）→ ✅ 走迷宫 v1.7（循环块组合深化）→ ✅ 走迷宫 v1.8（嵌套循环深化）→ ✅ 走迷宫 v1.9（关卡编辑器）