# 益智 · 编程教学 子系列

> 编程教学线（D1 原型）：把「推迟」的玩法逻辑转型为有标准答案的编程练习（指导原则 §一之二）。
> 定位：指令序列编程启蒙（对标 Scratch 指令序列阶段），纯离线 zip + 有标准答案判定。
> 首个工具：机器人走迷宫（2026-09-22，复用推箱子关卡/解析）。
> 立项依据：`docs/教育工具/编程教学产品线-立项评估.md`。
> 更新：2026-09-22

## 子系列内工具

| 工具 | 状态 | 版本 | 说明 |
|------|------|------|------|
| 机器人走迷宫 | ✅ 已构建 | v1.7 | **指令序列编程**：编排 ↑前进/↰左转/↱右转/**🧱探测**/**➡N步**/**❓if墙**/**🔁循环** → 执行 → 目标盘面判定；迷宫 = 推箱子关卡（Microban 155 关 + **v1.5 条件分支 20 关（156-175）+ v1.6 参数化 10 关（176-185）+ v1.7 循环块 10 关（186-195）**，简单 1-30/普通 31-80/挑战 81-195 全量开放）；**编程核心概念七件套：顺序（v1.0）→ 循环（v1.1，chip ×2/×3/×4）→ 条件（v1.3，🧱 前方探测）→ 调试（v1.4，执行高亮+失败定位+差箱提示+执行锁定）→ 分支（v1.5，❓ if前方有墙，then/else 分支 + editCtx 编辑区 + 工作队列执行器 + 嵌套 if ≤2 层）→ 参数化（v1.6，➡ 走N步，一次直线移动 N 格，共享 tryMoveFwd 移动函数）→ 循环块（v1.7，🔁 循环，一组指令重复 N 次，块内可嵌 if/steps 组合控制结构，editCtx type 扩展 + ifDepth 守卫 + execLoop 轮间重置）**；星级（指令步数 vs 最优解 b，if 计 1+分支，loop 计 1+body 递归，v1.6/v1.7 新关 b=含 steps/循环块最优编写量）；复用推箱子 levels.js/解析/推箱逻辑零素材。文档 `docs/工具文档/益智/编程教学/机器人走迷宫-{设计,使用}文档.md` |
| 方块排列指令 | ✅ 已构建 | v1.0 | **构建型布局编程**（与走迷宫移动型互补）：给定目标布局（K 个四格骨牌堆成），为每块编排指令（↻ 旋转 ×N / ⬅➡ 移动 N 格 / ⬇ 落下）→ 执行落定 → 布局逐格比对判定；**概念：顺序（逐块）+ 旋转（空间变换）+ 参数化移动 + 空间规划**；程序化生成器必有解（生成即执行）+ 最优解星级；逐块即时差格反馈（「这一块有 N 格放偏了」）；复用俄罗斯方块 SHAPES 几何零素材。文档 `docs/工具文档/益智/编程教学/方块排列指令-{设计,使用}文档.md` |
| 合并策略编程 | ✅ 已构建 | v1.0 | **策略型多步规划**（与走迷宫移动型/方块排列构建型互补）：确定性初始盘面（去 2048 随机新块）+ 方向指令序列（⬅➡⬆⬇ 参数化 ×N）→ 执行 → 最终盘面 ≥ 目标数字判定（8/16/32 三档）；**概念：顺序 + 循环（×N）+ 策略规划（多步合并路径预判）**；**BFS 最短指令数星级**（确定性状态空间可算，纯函数 applyMovePure BFS/执行器共用）；生成器必有解（sum≥target 原子量硬约束 + BFS 必达 + seed 兜底）；复用 2048 合并逻辑零素材。文档 `docs/工具文档/益智/编程教学/合并策略编程-{设计,使用}文档.md` |
| 循环指令 | ✅ 已构建 | v1.0 | **循环结构专训（显式循环块）**：走廊盘面（轨迹格空地 + 相邻非轨迹格墙，堵死捷径唯一路径）+ 机器人 S→G + `🔁 重复 N 次 {一组指令}`（**区别于走迷宫隐式 chip ×N**：走迷宫 = 一个动作做多遍；循环块 = 一组动作做多遍，循环 = 自动化）；**编写量星级**（循环块 1 条 + 内部指令计 1 不乘 N；走廊无捷径 + 展开写必 ≥1.5×optimal 封顶 2★ → 循环块 = 唯一 3★ 激励验证 600/600）；骨架 pattern（fwd×k+right 螺旋无近路）+ simulate 生成器/执行器共用；oracle 修订（走廊堵捷径/自交检查/起点固定/循环块级失败定位）。文档 `docs/工具文档/益智/编程教学/循环指令-{设计,使用}文档.md` |

## 子系列原则

1. **有标准答案判定**：执行结果盘面 vs 目标盘面（程序可判，非表现性分数）——合规安全区核心。
2. **编程思维培养**：编排 → 执行 → 观察 → 调试（顺序/规划/优化），非直接控制玩法。
3. **复用优先**：关卡/解析/求解基准复用推箱子资产；概念关卡演进（顺序 → 循环 → 条件）。
4. **零素材**：SVG 程序化盘面，符合离线包体目标。
5. **低龄友好渐进**：不一步上完整 if/else → 先用「🧱 前方探测」（单条件判断）入门，
   v1.5 起「❓ if前方有墙」（完整分支结构）结合岔路口关卡渐进（简单单岔 → 挑战嵌套）；
   v1.6 起「➡ 走N步」（参数化移动）结合长直道关卡（参数 vs 循环概念区分）；
   v1.7 起「🔁 循环」（显式循环块）结合循环专关（一组动作自动化，块内可嵌 if/steps 组合）。

## 规划

- ✅ 顺序指令（v1.0）→ ✅ 循环指令（v1.1）→ ✅ 条件指令「前方探测」（v1.3）→ ✅ 分支指令「if前方有墙」（v1.5）→ ✅ 参数指令「走N步」（v1.6）→ ✅ 循环块「🔁循环」（v1.7）——**编程核心概念七块闭环完成**
- ✅ 方块排列指令 v1.0（D3，构建型布局编程：旋转 + 参数化移动）
- ✅ 合并策略编程 v1.0（D4，策略型多步规划：BFS 最短解）
- ✅ 循环指令 v1.0（D5，循环结构专训：显式循环块 + 走廊）
- 后续候选：嵌套循环（循环块内嵌循环块）、关卡编辑器（孩子自建迷宫题）
- 转型候选（矩阵 §10 阶段 D）：✅ 方块排列指令（俄罗斯方块）→ ✅ 合并策略编程（2048）→ ✅ 循环指令（反应测速，**推迟款转型全部完成**）→ ✅ 走迷宫 v1.5（条件分支深化）→ ✅ 走迷宫 v1.6（参数化移动深化）→ ✅ 走迷宫 v1.7（循环块组合深化）